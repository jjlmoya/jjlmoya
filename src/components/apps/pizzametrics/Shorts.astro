---
interface Props {
    shortIds: string[];
}
const { shortIds } = Astro.props;
---

<section class="py-32 px-6 bg-[#1A1C2C] text-white overflow-hidden">
    <div class="max-w-7xl mx-auto">
        <div class="text-center mb-24">
            <h2 class="text-4xl md:text-7xl font-black tracking-tighter uppercase italic mb-8">
                La App en <span class="text-[#E63946]">Movimiento</span>
            </h2>
            <div class="w-24 h-1 bg-[#E63946] mx-auto mb-8"></div>
            <p class="text-xl md:text-2xl font-light text-white/60 italic max-w-2xl mx-auto">
                Echa un vistazo rápido a cómo Pizzametrics transforma tu cocina en un laboratorio de
                precisión.
            </p>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-8">
            {
                shortIds.map((id) => (
                    <div class="relative group">
                        <div class="relative aspect-[9/16] rounded-[2.5rem] overflow-hidden border-4 border-white/10 shadow-2xl transition-all duration-700 hover:scale-[1.02] hover:border-[#E63946]/50">
                            <iframe
                                src={`https://www.youtube.com/embed/${id}?rel=0&modestbranding=1&autohide=1&muted=1&controls=0`}
                                title="Reproductor de YouTube Shorts"
                                class="absolute inset-0 w-full h-full opacity-90 group-hover:opacity-100"
                                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                                allowfullscreen
                            />
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>
