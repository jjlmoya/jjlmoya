---
import { Icon } from "astro-icon/components";

const googlePlayUrl = "https://play.google.com/store/apps/details?id=com.gamebob.lexicrash";
---

<section class="py-48 px-6 text-center relative z-10 overflow-hidden">
    <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-[#00D9FF] opacity-[0.03] blur-[150px] rounded-full"
    >
    </div>

    <div class="max-w-4xl mx-auto relative">
        <h2
            class="text-7xl md:text-9xl font-black mb-12 tracking-tighter leading-none arcade-title"
        >
            ¿EVITARÁS EL<br /><span class="text-[#FFD700] glow-text-yellow px-4">COLAPSO?</span>
        </h2>

        <p class="text-2xl md:text-3xl text-white/50 mb-16 font-medium balance-text">
            El reto diario está activo. No dejes que las letras ganen la partida hoy.
        </p>

        <div class="flex flex-col items-center gap-8">
            <a href={googlePlayUrl} target="_blank" class="arcade-btn-ultimate group">
                <div
                    class="absolute inset-0 bg-gradient-to-r from-[#00D9FF] to-[#0088CC] opacity-100 group-hover:scale-110 transition-transform duration-500"
                >
                </div>
                <div class="relative flex items-center gap-6 px-12 py-8 md:px-20 md:py-10">
                    <Icon name="mdi:google-play" class="w-12 h-12 md:w-16 md:h-16" />
                    <div class="text-left">
                        <span
                            class="block text-xs md:text-sm font-black uppercase tracking-[0.3em] opacity-70 mb-1"
                            >Disponible Gratis en</span
                        >
                        <span class="text-4xl md:text-6xl font-black tracking-tighter"
                            >Google Play</span
                        >
                    </div>
                </div>
            </a>

            <div class="flex flex-col items-center gap-2">
                <div class="flex gap-1">
                    {
                        [1, 2, 3, 4, 5].map(() => (
                            <Icon name="mdi:star" class="w-6 h-6 text-[#FFD700]" />
                        ))
                    }
                </div>
                <span class="text-sm font-black uppercase tracking-widest text-white/30">
                    +4.8/5 valorado por la comunidad
                </span>
            </div>
        </div>
    </div>
</section>

<style>
    .arcade-title {
        text-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
    }

    .glow-text-yellow {
        text-shadow:
            0 0 40px rgba(255, 215, 0, 0.6),
            0 0 80px rgba(255, 215, 0, 0.3);
    }

    .arcade-btn-ultimate {
        position: relative;
        display: inline-block;
        border-radius: 2.5rem;
        overflow: hidden;
        text-decoration: none;
        color: white;
        transition: all 0.5s cubic-bezier(0.19, 1, 0.22, 1);
        box-shadow: 0 30px 60px -15px rgba(0, 217, 255, 0.5);
        border: 2px solid rgba(255, 255, 255, 0.2);
    }

    .arcade-btn-ultimate:hover {
        transform: translateY(-10px) scale(1.02);
        box-shadow: 0 40px 80px -15px rgba(0, 217, 255, 0.7);
        border-color: rgba(255, 255, 255, 0.4);
    }

    .balance-text {
        text-wrap: balance;
    }
</style>
