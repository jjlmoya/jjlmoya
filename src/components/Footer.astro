---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<footer class={`py-8 text-center text-sm opacity-60 transition-colors duration-300 ${className}`}>
    <p>&copy; {new Date().getFullYear()}</p>
</footer>
