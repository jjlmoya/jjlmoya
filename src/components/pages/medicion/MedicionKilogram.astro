---

---

<section
    class="min-h-screen flex flex-col items-center justify-center relative py-32 bg-[#111827] text-[#e5e7eb] overflow-hidden"
>
    <!-- Metallic/Glass Background -->
    <div class="absolute inset-0 pointer-events-none opacity-20">
        <svg class="w-full h-full">
            <defs>
                <linearGradient id="metalGrad" x1="0" y1="0" x2="1" y2="1">
                    <stop offset="0%" stop-color="#374151"></stop>
                    <stop offset="50%" stop-color="#1f2937"></stop>
                    <stop offset="100%" stop-color="#111827"></stop>
                </linearGradient>
            </defs>
            <rect width="100%" height="100%" fill="url(#metalGrad)"></rect>
        </svg>
    </div>

    <div class="container mx-auto px-6 max-w-4xl relative z-10">
        <div class="flex flex-col md:flex-row-reverse gap-16 items-center">
            <div class="w-full md:w-1/2 space-y-8">
                <span
                    class="text-[#9ca3af] font-mono text-xs tracking-widest uppercase border-b border-[#4b5563] pb-2"
                    >El Objeto Sagrado</span
                >
                <h2 class="text-4xl md:text-5xl font-serif italic text-[#f3f4f6]">Le Grand K</h2>

                <p class="text-lg leading-relaxed font-serif text-[#d1d5db]/90">
                    Durante 130 años, el "kilogramo" no fue una idea. Fue un objeto. Un cilindro de
                    platino e iridio guardado bajo tres campanas de cristal en París.
                </p>

                <div
                    class="bg-[#1f2937]/60 p-6 rounded-sm border border-[#374151] mt-4 backdrop-blur-sm"
                >
                    <h3 class="text-[#e5e7eb] font-bold mb-2">La Fragilidad de la Materia</h3>
                    <p class="text-sm text-[#9ca3af] leading-relaxed">
                        Si alguien estornudaba sobre él, el universo entero "perdía" peso. Si perdía
                        un átomo, todo lo demás tenía que redefinirse.
                    </p>
                    <p class="mt-4 text-sm text-[#d1d5db] italic">
                        En 2019 lo jubilamos. Ahora el kilo se define por la constante de Planck.
                        Pasamos de adorar un ídolo de metal a adorar una constante matemática.
                    </p>
                </div>
            </div>

            <div class="w-full md:w-1/2 flex justify-center relative h-[400px]">
                <!-- IPK Visualization (SVG) -->
                <div class="relative w-full h-full flex items-center justify-center">
                    <svg viewBox="0 0 400 300" class="w-full h-full overflow-visible">
                        <defs>
                            <linearGradient id="glassShine" x1="0" y1="0" x2="1" y2="0">
                                <stop offset="0%" stop-color="rgba(255,255,255,0.1)"></stop>
                                <stop offset="50%" stop-color="rgba(255,255,255,0.4)"></stop>
                                <stop offset="100%" stop-color="rgba(255,255,255,0.1)"></stop>
                            </linearGradient>
                            <linearGradient id="platinum" x1="0" y1="0" x2="1" y2="0">
                                <stop offset="0%" stop-color="#9ca3af"></stop>
                                <stop offset="50%" stop-color="#f3f4f6"></stop>
                                <stop offset="100%" stop-color="#6b7280"></stop>
                            </linearGradient>
                        </defs>

                        <!-- Base -->
                        <ellipse
                            cx="200"
                            cy="250"
                            rx="100"
                            ry="20"
                            fill="#1f2937"
                            stroke="#374151"
                            stroke-width="2"></ellipse>

                        <!-- Bell Jar 1 (Outer) -->
                        <path
                            d="M 120 250 L 120 100 Q 120 20 200 20 Q 280 20 280 100 L 280 250"
                            fill="url(#glassShine)"
                            stroke="#e5e7eb"
                            stroke-width="1"
                            opacity="0.3"></path>

                        <!-- Bell Jar 2 (Inner) -->
                        <path
                            d="M 140 250 L 140 120 Q 140 50 200 50 Q 260 50 260 120 L 260 250"
                            fill="none"
                            stroke="#e5e7eb"
                            stroke-width="1"
                            opacity="0.2"></path>

                        <!-- The Kilogram (Cylinder) -->
                        <g transform="translate(200, 220)">
                            <!-- Cylinder Body -->
                            <path d="M -20 0 L -20 -40 L 20 -40 L 20 0" fill="url(#platinum)"
                            ></path>
                            <!-- Cylinder Top -->
                            <ellipse cx="0" cy="-40" rx="20" ry="5" fill="#e5e7eb"></ellipse>
                            <!-- Cylinder Bottom (Curve) -->
                            <path d="M -20 0 Q 0 10 20 0" fill="url(#platinum)"></path>

                            <!-- Glow/Halo -->
                            <circle
                                cx="0"
                                cy="-20"
                                r="30"
                                fill="white"
                                opacity="0.1"
                                class="animate-pulse-halo"></circle>
                        </g>

                        <!-- Reflection -->
                        <path
                            d="M 120 250 L 130 260 L 270 260 L 280 250"
                            fill="url(#glassShine)"
                            opacity="0.1"></path>
                    </svg>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    @keyframes pulseHalo {
        0% {
            opacity: 0.1;
            transform: scale(1);
        }
        50% {
            opacity: 0.3;
            transform: scale(1.2);
        }
        100% {
            opacity: 0.1;
            transform: scale(1);
        }
    }
    .animate-pulse-halo {
        animation: pulseHalo 4s ease-in-out infinite;
    }
</style>
