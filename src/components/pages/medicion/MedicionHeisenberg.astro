---

---

<section
    class="min-h-screen flex flex-col items-center justify-center relative py-32 bg-neutral-950 text-neutral-200"
>
    <div class="container mx-auto px-6 max-w-4xl">
        <div class="flex flex-col md:flex-row-reverse gap-16 items-center">
            <div class="w-full md:w-1/2 space-y-8">
                <h2 class="text-4xl md:text-5xl font-serif italic text-white">
                    El Principio de Incertidumbre
                </h2>

                <p class="text-lg leading-relaxed font-serif text-neutral-400">
                    En el reino cuántico, la medición deja de ser un acto pasivo. Para "ver" un
                    electrón, debes golpearlo con un fotón.
                </p>
                <p class="text-lg leading-relaxed font-serif text-neutral-400">
                    El acto de medir altera lo medido. No puedes conocer la posición y la velocidad
                    al mismo tiempo con precisión absoluta.
                </p>

                <div class="space-y-4 mt-8">
                    <div class="bg-neutral-900 p-6 rounded-sm border-l-2 border-purple-500/50">
                        <p class="text-sm text-neutral-300 leading-relaxed">
                            <strong>Heisenberg:</strong> La naturaleza tiene un límite fundamental de
                            resolución. El universo no es infinitamente nítido; es borroso en su base.
                        </p>
                    </div>
                </div>
            </div>

            <div class="w-full md:w-1/2 flex justify-center">
                <div
                    class="relative w-64 h-64 md:w-80 md:h-80 bg-black rounded-full flex items-center justify-center border border-neutral-800 overflow-hidden group"
                >
                    
                    <div
                        class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(168,85,247,0.4),transparent_70%)] blur-xl transition-all duration-300 group-hover:scale-150 group-hover:opacity-50"
                    >
                    </div>

                    
                    <div
                        class="w-4 h-4 bg-white rounded-full shadow-[0_0_20px_rgba(255,255,255,0.8)] transition-all duration-75 ease-out"
                        id="quantum-particle"
                    >
                    </div>

                    <div
                        class="absolute bottom-8 text-[10px] font-mono uppercase tracking-widest text-neutral-600"
                    >
                        Intentando localizar...
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    const container = document.querySelector(".group");
    const particle = document.getElementById("quantum-particle");

    if (container && particle) {
        container.addEventListener("mousemove", () => {
            

            
            
            
            

            
            const jitterX = (Math.random() - 0.5) * 50;
            const jitterY = (Math.random() - 0.5) * 50;

            particle.style.transform = `translate(${jitterX}px, ${jitterY}px)`;
        });

        container.addEventListener("mouseleave", () => {
            particle.style.transform = `translate(0, 0)`;
        });
    }
</script>
