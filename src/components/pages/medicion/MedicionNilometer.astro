---

---

<section
    class="min-h-screen flex flex-col items-center justify-center relative py-32 bg-[#0c1218] text-[#a8b3cf] overflow-hidden"
>
    
    <div class="absolute inset-0 opacity-20 pointer-events-none">
        <div
            class="absolute bottom-0 w-full h-1/2 bg-gradient-to-t from-[#1e3a8a] to-transparent animate-tide"
        >
        </div>
    </div>

    <div class="container mx-auto px-6 max-w-4xl relative z-10">
        <div class="flex flex-col md:flex-row gap-16 items-center">
            <div class="w-full md:w-1/2 space-y-8">
                <span
                    class="text-[#60a5fa] font-mono text-xs tracking-widest uppercase border-b border-[#1e40af] pb-2"
                    >La Medida de la Vida</span
                >
                <h2 class="text-4xl md:text-5xl font-serif italic text-[#e0f2fe]">El Nilómetro</h2>

                <p class="text-lg leading-relaxed font-serif text-[#cbd5e1]/90">
                    En el Antiguo Egipto, medir no era solo curiosidad; era supervivencia. Cada año,
                    el Nilo decidía el destino del imperio.
                </p>

                <div
                    class="bg-[#172554]/40 p-6 rounded-sm border-l-2 border-[#3b82f6] mt-4 backdrop-blur-sm"
                >
                    <h3 class="text-[#93c5fd] font-bold mb-2">La Escala del Faraón</h3>
                    <p class="text-sm text-[#dbeafe] leading-relaxed">
                        Construyeron pozos con columnas graduadas para medir la crecida del río. El
                        nivel del agua dictaba los impuestos del año siguiente.
                    </p>
                    <ul class="mt-4 space-y-2 text-xs font-mono text-[#60a5fa]">
                        <li class="flex items-center gap-2">
                            <div class="w-2 h-2 bg-red-500 rounded-full"></div>
                            <span>12 Codos: Hambre (Sequía)</span>
                        </li>
                        <li class="flex items-center gap-2">
                            <div class="w-2 h-2 bg-green-500 rounded-full"></div>
                            <span>14 Codos: Abundancia (Impuestos altos)</span>
                        </li>
                        <li class="flex items-center gap-2">
                            <div class="w-2 h-2 bg-red-500 rounded-full"></div>
                            <span>16 Codos: Desastre (Inundación)</span>
                        </li>
                    </ul>
                </div>
            </div>

            <div class="w-full md:w-1/2 flex justify-center relative h-[500px]">
                
                <div
                    class="relative w-48 h-full bg-[#0f172a] border-x-2 border-[#1e293b] flex justify-center overflow-hidden"
                >
                    
                    <div
                        class="absolute top-0 bottom-0 w-16 bg-[#1e293b] flex flex-col items-center justify-between py-4 border-x border-[#334155]"
                    >
                        {
                            Array.from({ length: 16 }).map((_, i) => (
                                <div class="w-full h-px bg-[#475569] relative">
                                    <span class="absolute right-full mr-2 text-[8px] font-mono text-[#64748b] -translate-y-1/2">
                                        {16 - i}
                                    </span>
                                </div>
                            ))
                        }
                    </div>

                    
                    <div
                        class="absolute bottom-0 left-0 right-0 bg-[#1d4ed8]/60 backdrop-blur-sm transition-all duration-1000 animate-flood border-t border-[#60a5fa]"
                    >
                        
                        <div class="absolute top-2 left-0 w-full h-px bg-white/20"></div>
                        <div class="absolute top-4 left-0 w-full h-px bg-white/10"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    @keyframes flood {
        0% {
            height: 20%;
        } /* Drought */
        50% {
            height: 85%;
        } /* Flood */
        70% {
            height: 60%;
        } /* Perfect */
        100% {
            height: 20%;
        }
    }
    .animate-flood {
        animation: flood 10s ease-in-out infinite;
    }

    @keyframes tide {
        0%,
        100% {
            transform: translateY(10px);
        }
        50% {
            transform: translateY(-10px);
        }
    }
    .animate-tide {
        animation: tide 5s ease-in-out infinite;
    }
</style>
