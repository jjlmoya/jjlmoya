---
---
<section class="min-h-screen flex flex-col items-center justify-center relative py-32 bg-[#022c22] text-[#ecfccb] overflow-hidden">
    
    <div class="absolute inset-0 pointer-events-none opacity-5">
        <div class="absolute top-10 left-10 text-9xl font-serif">$</div>
        <div class="absolute bottom-20 right-20 text-9xl font-serif">€</div>
        <div class="absolute top-40 right-40 text-9xl font-serif">£</div>
        <div class="absolute bottom-40 left-40 text-9xl font-serif">¥</div>
    </div>

    <div class="container mx-auto px-6 max-w-4xl relative z-10">
        <div class="flex flex-col md:flex-row-reverse gap-16 items-center">
            
            <div class="w-full md:w-1/2 space-y-8">
                <span class="text-[#84cc16] font-mono text-xs tracking-widest uppercase border-b border-[#3f6212] pb-2">La Medida del Deseo</span>
                <h2 class="text-4xl md:text-5xl font-serif italic text-[#f7fee7]">El Dinero</h2>
                
                <p class="text-lg leading-relaxed font-serif text-[#d9f99d]/90">
                    No mide átomos ni segundos. Mide confianza. 
                    Es una alucinación compartida que nos permite colaborar con extraños.
                </p>
                
                <div class="bg-[#064e3b]/50 p-6 rounded-sm border border-[#65a30d]/30 mt-4 backdrop-blur-sm">
                    <h3 class="text-[#ecfccb] font-bold mb-2">Del Oro al Bit</h3>
                    <p class="text-sm text-[#bef264] leading-relaxed">
                        Empezamos pesando metales. Luego usamos papel que representaba metal. 
                        Ahora movemos dígitos en servidores. La materia ha desaparecido, solo queda el valor puro.
                    </p>
                    <p class="mt-4 text-sm text-[#84cc16] italic">
                        Es la métrica más universal y la única capaz de convertir el tiempo de un hombre en la cena de otro.
                    </p>
                </div>
            </div>
            
            <div class="w-full md:w-1/2 flex justify-center relative h-[400px] perspective-1000">
                
                <div class="coin-wrapper relative w-64 h-64 flex items-center justify-center">
                    <div class="coin animate-spin-3d">
                        
                        {Array.from({ length: 16 }).map((_, i) => {
                            
                            const zOffset = (i - 7.5) * 1.5; 
                            const isFace = i === 0 || i === 15;
                            const brightness = isFace ? 1 : 0.7; 
                            
                            return (
                                <div class="absolute inset-0 coin-layer" style={`--z: ${zOffset}px; --brightness: ${brightness};`}>
                                    <svg viewBox="0 0 200 200" class="w-full h-full">
                                        <defs>
                                            <linearGradient id={`goldGrad${i}`} x1="0%" y1="0%" x2="100%" y2="100%">
                                                <stop offset="0%" stop-color="#fcd34d" />
                                                <stop offset="50%" stop-color="#d97706" />
                                                <stop offset="100%" stop-color="#b45309" />
                                            </linearGradient>
                                        </defs>
                                        
                                        
                                        <circle cx="100" cy="100" r="95" fill={`url(#goldGrad${i})`} stroke="#78350f" stroke-width={isFace ? 2 : 0} />
                                        
                                        
                                        {isFace && (
                                            <g>
                                                
                                                <circle cx="100" cy="100" r="85" fill="none" stroke="#78350f" stroke-width="1" opacity="0.5" />
                                                <circle cx="100" cy="100" r="80" fill="none" stroke="#78350f" stroke-width="3" stroke-dasharray="1 5" opacity="0.3" />
                                                
                                                
                                                <g transform="translate(100, 100)">
                                                    
                                                    {Array.from({ length: 12 }).map((_, j) => (
                                                        <path d="M 0 -60 L 10 -20 L 0 -15 L -10 -20 Z" fill="#78350f" opacity="0.8" transform={`rotate(${j * 30})`} />
                                                    ))}
                                                    
                                                    <circle cx="0" cy="0" r="25" fill="#78350f" opacity="0.9" />
                                                    <circle cx="0" cy="0" r="15" fill="#fcd34d" opacity="0.4" />
                                                </g>
                                            </g>
                                        )}
                                    </svg>
                                </div>
                            );
                        })}
                    </div>
                    
                    
                    <div class="absolute inset-0 pointer-events-none animate-digital-pulse z-20">
                        <svg viewBox="0 0 400 400" class="w-full h-full">
                             <g transform="translate(100, 100) scale(0.5)"> 
                                <circle cx="200" cy="200" r="140" fill="none" stroke="#4ade80" stroke-width="2" opacity="0.3" stroke-dasharray="20 10" />
                                <circle cx="200" cy="80" r="8" fill="#4ade80" />
                                <circle cx="320" cy="200" r="8" fill="#4ade80" />
                                <circle cx="200" cy="320" r="8" fill="#4ade80" />
                                <circle cx="80" cy="200" r="8" fill="#4ade80" />
                                <line x1="200" y1="80" x2="320" y2="200" stroke="#4ade80" stroke-width="2" opacity="0.3" />
                                <line x1="320" y1="200" x2="200" y2="320" stroke="#4ade80" stroke-width="2" opacity="0.3" />
                                <line x1="200" y1="320" x2="80" y2="200" stroke="#4ade80" stroke-width="2" opacity="0.3" />
                                <line x1="80" y1="200" x2="200" y2="80" stroke="#4ade80" stroke-width="2" opacity="0.3" />
                            </g>
                        </svg>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

<style>
    .perspective-1000 {
        perspective: 1000px;
    }
    
    .coin-wrapper {
        transform-style: preserve-3d;
    }

    .coin {
        position: relative;
        width: 100%;
        height: 100%;
        transform-style: preserve-3d;
    }

    .coin-layer {
        backface-visibility: visible; 
        transform: translateZ(var(--z));
        filter: brightness(var(--brightness));
    }

    @keyframes spin3d {
        0% { transform: rotateY(0deg) rotateX(10deg); }
        100% { transform: rotateY(360deg) rotateX(10deg); }
    }
    .animate-spin-3d {
        animation: spin3d 6s linear infinite;
    }

    @keyframes digitalPulse {
        0% { opacity: 0; transform: scale(1); }
        50% { opacity: 1; transform: scale(1.1); }
        100% { opacity: 0; transform: scale(1.2); }
    }
    .animate-digital-pulse {
        animation: digitalPulse 4s ease-in-out infinite;
    }
</style>
