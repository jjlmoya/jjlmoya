---
interface Props {
    side: "left" | "right";
}
const { side } = Astro.props;
---

{
    side === "left" ? (
        <div class="flex items-center justify-center h-full w-full relative overflow-hidden bg-[#0c0a09]">
            <div class="relative w-full h-full flex items-center justify-center">
                <div class="relative w-32 h-64 flex flex-col items-center justify-end overflow-hidden">
                    <div class="absolute bottom-0 w-full h-full">
                        <svg
                            class="absolute bottom-4 right-8 w-8 h-12 text-[#e7e5e4] opacity-0 animate-step-r"
                            viewBox="0 0 100 250"
                            style="animation-delay: 0s;"
                        >
                            <path
                                fill="currentColor"
                                d="M30,0 C50,0 70,20 70,50 L70,180 C70,220 50,250 30,250 C10,250 0,220 0,180 L0,50 C0,20 10,0 30,0 Z"
                            />
                        </svg>

                        <svg
                            class="absolute bottom-20 left-8 w-8 h-12 text-[#e7e5e4] opacity-0 animate-step-l"
                            viewBox="0 0 100 250"
                            style="animation-delay: 1s;"
                        >
                            <path
                                fill="currentColor"
                                d="M30,0 C50,0 70,20 70,50 L70,180 C70,220 50,250 30,250 C10,250 0,220 0,180 L0,50 C0,20 10,0 30,0 Z"
                            />
                        </svg>

                        <svg
                            class="absolute bottom-36 right-8 w-8 h-12 text-[#e7e5e4] opacity-0 animate-step-r"
                            viewBox="0 0 100 250"
                            style="animation-delay: 2s;"
                        >
                            <path
                                fill="currentColor"
                                d="M30,0 C50,0 70,20 70,50 L70,180 C70,220 50,250 30,250 C10,250 0,220 0,180 L0,50 C0,20 10,0 30,0 Z"
                            />
                        </svg>

                        <svg
                            class="absolute bottom-52 left-8 w-8 h-12 text-[#e7e5e4] opacity-0 animate-step-l"
                            viewBox="0 0 100 250"
                            style="animation-delay: 3s;"
                        >
                            <path
                                fill="currentColor"
                                d="M30,0 C50,0 70,20 70,50 L70,180 C70,220 50,250 30,250 C10,250 0,220 0,180 L0,50 C0,20 10,0 30,0 Z"
                            />
                        </svg>
                    </div>
                </div>
            </div>
        </div>
    ) : (
        <div class="flex flex-col items-center justify-center h-full text-center space-y-6 max-w-md mx-auto">
            <h3 class="font-serif text-3xl text-orange-500/60 uppercase tracking-widest">
                El Camino
            </h3>

            <div class="space-y-2">
                <div
                    class="font-variant-numeric tabular-nums text-4xl md:text-5xl font-bold text-[#e7e5e4]"
                    id="steps-count"
                >
                    0
                </div>
                <div class="text-sm text-[#e7e5e4]/40 uppercase tracking-wider">Pasos Dados</div>
            </div>

            <p
                class="text-xl leading-relaxed text-[#e7e5e4]"
                style="font-family: 'Cinzel', serif; font-variant: small-caps;"
            >
                ...
                <span id="steps-km" class="text-orange-500">
                    0 KM
                </span>{" "}
                RECORRIDOS. AL FINAL LLEGÁIS AL MISMO SITIO, PERO PARECE QUE OS GUSTA DAR RODEOS.
                SUPONGO QUE ESO ES VIVIR. YO PREFIERO VIAJAR A CABALLO. EL MÍO SE LLAMA BINKY. ES UN
                BUEN CHICO.
            </p>
        </div>
    )
}

<style>
    @keyframes step-appear {
        0% {
            opacity: 0;
            transform: scale(0.9);
        }
        20% {
            opacity: 0.6;
            transform: scale(1);
        }
        80% {
            opacity: 0.6;
            transform: scale(1);
        }
        100% {
            opacity: 0;
            transform: scale(0.95);
        }
    }

    .animate-step-l {
        animation: step-appear 4s ease-in-out infinite;
    }
    .animate-step-r {
        animation: step-appear 4s ease-in-out infinite;
    }
</style>

<script>
    import { calculateStepsStats } from "../../../../utils/libraryLogic";

    const countEl = document.getElementById("steps-count");
    const kmEl = document.getElementById("steps-km");

    document.addEventListener("update-life-stats", (e: any) => {
        const { stats } = e.detail;
        if (!countEl || !stats) return;

        const { stepsTaken } = calculateStepsStats(stats.daysAlive);

        const km = (stepsTaken * 0.762) / 1000;

        countEl.innerText = stepsTaken.toLocaleString();
        if (kmEl) kmEl.innerText = `${Math.floor(km).toLocaleString()} KM`;
    });
</script>
