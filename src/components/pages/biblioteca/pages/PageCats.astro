---
import GameBobLogo from "../../../GameBobLogo.astro";

interface Props {
    side: 'left' | 'right';
}
const { side } = Astro.props;
---

{side === 'left' ? (
    <div class="flex items-center justify-center h-full w-full relative overflow-hidden bg-[#0c0a09]">
        
        <div class="relative w-full h-full flex items-center justify-center">
             <div class="w-64 h-64 relative flex items-center justify-center">
                <div class="absolute inset-0 bg-yellow-500/10 blur-3xl rounded-full animate-pulse"></div>
                <div class="w-full h-full opacity-80 drop-shadow-[0_0_15px_rgba(234,179,8,0.3)]">
                    <GameBobLogo />
                </div>
             </div>
        </div>
    </div>
) : (
    <div class="flex flex-col items-center justify-center h-full text-center space-y-6 max-w-md mx-auto">
        <h3 class="font-serif text-3xl text-yellow-600/60 uppercase tracking-widest">Los Gatos</h3>
        
        <div class="space-y-2">
            <div class="font-variant-numeric tabular-nums text-5xl md:text-6xl font-bold text-[#e7e5e4]">
                ∞
            </div>
            <div class="text-sm text-[#e7e5e4]/40 uppercase tracking-wider">Importancia Relativa</div>
        </div>
        
        <p class="text-sm md:text-base leading-normal text-[#e7e5e4]" style="font-family: 'Cinzel', serif; font-variant: small-caps;">
            LOS GATOS. LOS GATOS SON AGRADABLES. <br/><br/>
            EL TIEMPO NO PASA IGUAL PARA ELLOS. MIENTRAS EL RESTO DEL MUNDO CORRE HACIA EL FINAL, ELLOS SIMPLEMENTE ESTÁN. SI TE SIENTES ABRUMADO, BUSCA UNO. EL RONRONEO ES LA ÚNICA COSA QUE EL UNIVERSO NO PUEDE DESTRUIR.
        </p>
    </div>
)}

<style>
    @keyframes blink-eyes {
        0%, 45%, 55%, 100% { transform: scaleY(1); }
        50% { transform: scaleY(0.1); }
    }
    .animate-blink-eyes {
        animation: blink-eyes 5s infinite;
    }
    
    @keyframes ping-slow {
        0% { transform: scale(0.8); opacity: 0.5; }
        100% { transform: scale(2); opacity: 0; }
    }
    .animate-ping-slow {
        animation: ping-slow 3s cubic-bezier(0, 0, 0.2, 1) infinite;
    }
</style>
