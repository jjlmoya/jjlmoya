<div class="bn-wrapper">
    <div class="bn-container">
        <button
            id="prev-page"
            class="bn-btn bn-btn-prev"
            style="touch-action: none; -webkit-tap-highlight-color: transparent;"
            aria-label="Página anterior"
        >
            <div class="bn-btn-gradient bn-gradient-prev"></div>
            <svg
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke-width="1.5"
                stroke="currentColor"
                class="bn-btn-icon bn-icon-prev"
            >
                <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5"
                ></path>
            </svg>
        </button>

        <button
            id="next-page"
            class="bn-btn bn-btn-next"
            style="touch-action: none; -webkit-tap-highlight-color: transparent;"
            aria-label="Página siguiente"
        >
            <div class="bn-btn-gradient bn-gradient-next"></div>
            <svg
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke-width="1.5"
                stroke="currentColor"
                class="bn-btn-icon bn-icon-next"
            >
                <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5"
                ></path>
            </svg>
        </button>
    </div>
</div>

<style>
    .bn-wrapper {
        position: fixed;
        inset: 0;
        pointer-events: none;
        z-index: 90;
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
        padding-bottom: 3rem;
    }

    @media (min-width: 768px) {
        .bn-wrapper {
            justify-content: center;
            padding-bottom: 0;
        }
    }

    .bn-container {
        width: 100%;
        max-width: 90rem;
        margin-left: auto;
        margin-right: auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-left: 1.5rem;
        padding-right: 1.5rem;
    }

    @media (min-width: 768px) {
        .bn-container {
            padding-left: 3rem;
            padding-right: 3rem;
        }
    }

    .bn-btn {
        pointer-events: auto;
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 4rem;
        height: 4rem;
        border-radius: 9999px;
        background-color: rgba(12, 10, 9, 0.5);
        backdrop-filter: blur(12px);
        border: 1px solid rgba(41, 37, 36, 0.5);
        color: #a8a29e;
        transition: all 0.3s;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
        cursor: pointer;
    }

    @media (min-width: 768px) {
        .bn-btn {
            width: 5rem;
            height: 5rem;
        }
    }

    .bn-btn:hover {
        color: #e7e5e4;
        border-color: rgba(120, 113, 108, 0.5);
        background-color: rgba(28, 25, 23, 0.8);
    }

    .bn-btn:active {
        transform: scale(0.95);
    }

    .bn-btn:disabled {
        opacity: 0;
        pointer-events: none;
        transform: translateY(1rem);
    }

    .bn-btn-gradient {
        position: absolute;
        inset: 0;
        border-radius: 9999px;
        opacity: 0;
        transition: opacity 0.5s;
    }

    .bn-gradient-prev {
        background-image: linear-gradient(to top right, rgba(255, 255, 255, 0.05), transparent);
    }

    .bn-gradient-next {
        background-image: linear-gradient(to top left, rgba(255, 255, 255, 0.05), transparent);
    }

    .bn-btn:hover .bn-btn-gradient {
        opacity: 1;
    }

    .bn-btn-icon {
        width: 1.5rem;
        height: 1.5rem;
        transition: transform 0.3s;
        position: relative;
        z-index: 10;
    }

    @media (min-width: 768px) {
        .bn-btn-icon {
            width: 2rem;
            height: 2rem;
        }
    }

    .bn-btn:hover .bn-icon-prev {
        transform: translateX(-0.25rem);
    }

    .bn-btn:hover .bn-icon-next {
        transform: translateX(0.25rem);
    }
</style>
