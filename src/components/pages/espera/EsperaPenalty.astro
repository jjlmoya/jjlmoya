<div
    id="penalty-state"
    class="espera-penalty"
>
    <h2 class="espera-penalty-title">La conexión se ha roto.</h2>
    <p class="espera-penalty-text">
        La atención es un hilo frágil. Al desviar la mirada, el momento se desvanece.
    </p>

    <button
        id="retry-btn"
        class="espera-penalty-btn"
    >
        Intentar de nuevo (Turno <span id="lost-ticket">001</span> perdido)
    </button>
</div>

<style>
    .espera-penalty {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(26, 26, 26, 0.95);
        backdrop-filter: blur(4px);
        z-index: 50;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: #f8f5f2;
        transition: opacity 0.7s;
        opacity: 0;
        padding-left: 1.5rem;
        padding-right: 1.5rem;
        text-align: center;
        box-sizing: border-box;
    }

    :global(.espera-penalty.hidden) {
        display: none !important;
    }

    .espera-penalty-title {
        font-size: 1.875rem;
        font-style: italic;
        font-weight: 300;
        margin: 0 0 1.5rem 0;
        line-height: 1.2;
    }

    @media (min-width: 768px) {
        .espera-penalty-title {
            font-size: 3rem;
        }
    }

    .espera-penalty-text {
        font-family: sans-serif;
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        opacity: 0.6;
        margin: 0 auto 3rem auto;
        max-width: 28rem;
        line-height: 1.625;
    }

    .espera-penalty-btn {
        font-family: sans-serif;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        border: none;
        border-bottom: 1px solid rgba(255, 255, 255, 0.3);
        background: transparent;
        color: inherit;
        padding-bottom: 0.5rem;
        cursor: pointer;
        transition: border-color 0.2s ease, color 0.2s ease;
    }

    .espera-penalty-btn:hover {
        border-bottom-color: #fff;
    }
</style>
