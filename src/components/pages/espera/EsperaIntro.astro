<div
    id="intro-state"
    class="espera-intro"
>
    <h1 class="espera-intro-title">
        El Arte de la Espera
    </h1>
    <div class="espera-intro-divider"></div>
    <p class="espera-intro-desc">
        Una experiencia de paciencia y presencia.<br />
        Sin distracciones. Solo tiempo.
    </p>

    <button
        id="ticket-btn"
        class="espera-intro-btn"
    >
        <span class="espera-intro-btn-text">
            Tomar Asiento
        </span>
    </button>

    <p
        class="espera-intro-note"
    >
        (Mantenga la ventana activa)
    </p>
</div>

<style>
    .espera-intro {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        transition: opacity 1s ease-in-out;
        width: 100%;
        max-width: 42rem;
        padding-left: 1.5rem;
        padding-right: 1.5rem;
        box-sizing: border-box;
    }

    .espera-intro-title {
        font-size: 3rem;
        font-style: italic;
        font-weight: 300;
        letter-spacing: -0.025em;
        margin: 0 0 2rem 0;
        color: #1a1a1a;
        line-height: 1.1;
    }

    @media (min-width: 768px) {
        .espera-intro-title {
            font-size: 4.5rem;
        }
    }

    .espera-intro-divider {
        width: 4rem;
        height: 1px;
        background-color: #333;
        margin-bottom: 2rem;
        opacity: 0.5;
    }

    .espera-intro-desc {
        font-size: 1.125rem;
        color: #666;
        margin: 0 0 3rem 0;
        line-height: 1.625;
    }

    @media (min-width: 768px) {
        .espera-intro-desc {
            font-size: 1.25rem;
        }
    }

    .espera-intro-btn {
        position: relative;
        padding: 1rem 3rem;
        overflow: hidden;
        border-radius: 9999px;
        border: 1px solid #333;
        background: transparent;
        cursor: pointer;
        transition: all 0.5s ease;
    }

    .espera-intro-btn:hover {
        background-color: #1a1a1a;
    }

    .espera-intro-btn-text {
        position: relative;
        z-index: 10;
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        font-family: sans-serif;
        color: #1a1a1a;
        transition: color 0.5s ease;
    }

    .espera-intro-btn:hover .espera-intro-btn-text {
        color: #fff;
    }

    .espera-intro-note {
        margin-top: 2rem;
        font-size: 0.75rem;
        font-family: sans-serif;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        color: #999;
        opacity: 0;
        animation: fade-in 1s ease-out forwards;
        animation-delay: 1s;
    }

    @keyframes fade-in {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
