<div
    id="success-state"
    class="espera-success"
>
    <h2 class="espera-success-title">Llegada.</h2>

    <div class="espera-success-stats">
        <div class="espera-success-stat">
            <span class="espera-success-val" id="final-time">00:00</span>
            <span class="espera-success-label">Transcurrido</span>
        </div>
        <div class="espera-success-stat">
            <span class="espera-success-val" id="final-attempts">1</span>
            <span class="espera-success-label">Intentos</span>
        </div>
    </div>

    <a
        href="/conceptos/"
        class="espera-success-link"
    >
        salir de la sala
    </a>
</div>

<style>
    .espera-success {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        background-color: #f8f5f2;
        z-index: 40;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        transition: opacity 1s;
        opacity: 0;
        padding-left: 1.5rem;
        padding-right: 1.5rem;
        text-align: center;
        box-sizing: border-box;
    }

    :global(.espera-success.hidden) {
        display: none !important;
    }

    .espera-success-title {
        font-size: 3rem;
        font-style: italic;
        font-weight: 300;
        margin: 0 0 3rem 0;
        color: #1a1a1a;
        line-height: 1.1;
    }

    @media (min-width: 768px) {
        .espera-success-title {
            font-size: 4.5rem;
        }
    }

    .espera-success-stats {
        display: flex;
        gap: 4rem;
        margin-bottom: 4rem;
        justify-content: center;
    }

    .espera-success-stat {
        text-align: center;
    }

    .espera-success-val {
        display: block;
        font-size: 2.25rem;
        font-weight: 300;
        margin-bottom: 0.5rem;
        color: #1a1a1a;
    }

    .espera-success-label {
        display: block;
        font-size: 10px;
        font-family: sans-serif;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        color: #888;
    }

    .espera-success-link {
        font-size: 0.875rem;
        text-decoration: none;
        color: #a3a3a3;
        border-bottom: 1px solid transparent;
        padding-bottom: 0.25rem;
        transition: color 0.2s ease, border-color 0.2s ease;
    }

    .espera-success-link:hover {
        color: #171717;
        border-bottom-color: #171717;
    }
</style>
