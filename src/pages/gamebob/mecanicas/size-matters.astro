---
import GameBobLayout from "../../../layouts/GameBobLayout.astro";
import { Icon } from "astro-icon/components";
---

<GameBobLayout
    title="Size Matters | GameBob"
    description="Prototipo de mecánica: El tamaño importa. Crece para romper, encoge para flotar."
>
    <div class="min-h-screen flex flex-col items-center justify-center relative overflow-hidden">
        <!-- Background Grid -->
        <div
            class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.03)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.03)_1px,transparent_1px)] bg-[size:40px_40px] [mask-image:radial-gradient(ellipse_at_center,black_40%,transparent_80%)] pointer-events-none"
        >
        </div>

        <div class="z-10 w-full max-w-4xl px-4">
            <header class="flex items-center justify-between mb-8">
                <a
                    href="/gamebob/mecanicas"
                    class="flex items-center gap-2 text-gray-400 hover:text-white transition-colors"
                >
                    <Icon name="mdi:arrow-left" class="w-5 h-5" />
                    <span>volver</span>
                </a>
                <h1 class="text-2xl font-bold text-white">
                    Size <span class="text-yellow-400">Matters</span>
                </h1>
            </header>

            <div
                class="relative aspect-[9/16] md:aspect-video bg-black/50 rounded-2xl overflow-hidden border border-white/10 shadow-2xl"
            >
                <canvas id="gameCanvas" class="w-full h-full touch-none"></canvas>

                <!-- Info Overlay -->
                <div class="absolute bottom-6 right-6 z-20 flex gap-2 pointer-events-none">
                    <a
                        href="https://github.com/jjlmoya/jjlmoya/blob/main/src/lib/mechanics/size-matters/SizeMattersGame.js"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="px-3 py-1.5 bg-black/80 hover:bg-black text-white rounded-lg text-xs font-mono flex items-center gap-2 transition-all hover:scale-105 backdrop-blur-sm border border-white/10 pointer-events-auto"
                        title="View source code"
                    >
                        <Icon name="mdi:github" class="w-4 h-4" />
                        <span>Code</span>
                    </a>
                    <a
                        href="https://github.com/jjlmoya/jjlmoya/blob/main/src/lib/mechanics/size-matters/README.md"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="px-3 py-1.5 bg-black/80 hover:bg-black text-white rounded-lg text-xs font-mono flex items-center gap-2 transition-all hover:scale-105 backdrop-blur-sm border border-white/10 pointer-events-auto"
                        title="View documentation"
                    >
                        <Icon name="mdi:file-document-outline" class="w-4 h-4" />
                        <span>Docs</span>
                    </a>
                </div>
            </div>

            <div class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="bg-white/5 p-6 rounded-xl border border-white/5">
                    <h3 class="text-lg font-bold text-white mb-2 flex items-center gap-2">
                        <Icon name="mdi:arrow-expand-all" class="w-5 h-5 text-yellow-400" />
                        Mecánica
                    </h3>
                    <p class="text-gray-400 text-sm leading-relaxed">
                        Mantén pulsado para <strong>CRECER</strong>. Te vuelves pesado y puedes
                        romper bloques rojos, pero caes rápido.
                        <br /><br />
                        Suelta para <strong>ENCOGER</strong>. Te vuelves ligero y flotas, ideal para
                        pasar por huecos estrechos azules.
                    </p>
                </div>
                <div class="bg-white/5 p-6 rounded-xl border border-white/5">
                    <h3 class="text-lg font-bold text-white mb-2 flex items-center gap-2">
                        <Icon name="mdi:code-tags" class="w-5 h-5 text-green-400" />
                        Detalles Técnicos
                    </h3>
                    <ul class="text-gray-400 text-sm space-y-2">
                        <li>• Física personalizada de masa variable.</li>
                        <li>• Gravedad dinámica basada en el tamaño.</li>
                        <li>• Generación procedural de obstáculos.</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</GameBobLayout>

<script>
    import { SizeMattersGame } from "../../../lib/mechanics/size-matters/SizeMattersGame";

    const canvas = document.getElementById("gameCanvas") as HTMLCanvasElement;

    // Resize canvas to match display size
    function resize() {
        if (game) game.resize();
    }

    window.addEventListener("resize", resize);

    // Start Game
    const game = new SizeMattersGame(canvas);
    // Initial resize is handled in constructor, but let's be safe
    game.resize();
</script>
