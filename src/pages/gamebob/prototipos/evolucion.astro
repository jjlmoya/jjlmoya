---
export const prerender = true;
import Layout from "../../../layouts/Layout.astro";
import { Icon } from "astro-icon/components";
import evolutionData from "../../../data/evolution-data.json";

const { base_elements, recipes } = evolutionData;
---

<Layout
    title="Evolución Infinita"
    description="Alquimia del caos. Combina elementos, crea criaturas y enfréntalas en la arena."
    bodyClass="ev-body"
    showBackground={false}
    mainClass="ev-main"
    headerClass="ev-header"
    image="/images/gamebob/prototipos/evolution.webp"
>
    <div class="ev-bg-layer">
        <div class="ev-bg-gradient"></div>
        <div class="ev-blob ev-blob-1"></div>
        <div class="ev-blob ev-blob-2"></div>
        <div class="ev-grid-pattern"></div>
    </div>

    <div class="ev-layout">
        <aside class="ev-sidebar">
            <div class="ev-sidebar-glow"></div>

            <div class="ev-sidebar-header">
                <h2 class="ev-sidebar-title">Laboratorio</h2>
                <p class="ev-sidebar-sub">Elementos Primordiales</p>
                <div class="ev-sidebar-mobile-icon">
                    <Icon name="mdi:flask" class="ev-sidebar-mobile-svg" />
                </div>
            </div>

            <div class="ev-sidebar-body">
                <button id="spawn-egg-btn" class="ev-spawn-btn">
                    <div class="ev-spawn-btn-overlay"></div>
                    <Icon name="mdi:egg" class="ev-spawn-btn-icon" />
                    <span class="ev-spawn-btn-text">Crear Huevo</span>
                </button>
            </div>

            <div class="ev-divider-section">
                <div class="ev-divider">
                    <div class="ev-divider-line"></div>
                    <span class="ev-divider-text">Elementos</span>
                    <div class="ev-divider-line"></div>
                </div>
            </div>

            <div id="inventory" class="ev-inventory"></div>

            <div class="ev-sidebar-footer">
                <button id="encyclopedia-btn" class="ev-btn ev-btn-encyclopedia">
                    <Icon name="mdi:book-open-page-variant" class="ev-btn-icon" />
                    <span class="ev-btn-text">Enciclopedia</span>
                </button>

                <button id="clear-btn" class="ev-btn ev-btn-clear">
                    <Icon name="mdi:delete-sweep" class="ev-btn-icon" />
                    <span class="ev-btn-text">Limpiar Todo</span>
                </button>
            </div>
        </aside>

        <main class="ev-playground" id="playground">
            <div class="ev-playground-texture"></div>

            <div id="stone-tutorial" class="ev-tutorial">
                <div class="ev-tutorial-inner">
                    <div class="ev-tutorial-row ev-tutorial-row-top">
                        <div class="ev-icon-group">
                            <Icon name="mdi:egg-outline" class="ev-egg-icon" />
                            <Icon name="mdi:fire" class="ev-egg-overlay ev-egg-overlay-fire" />
                        </div>
                        <div class="ev-icon-group">
                            <Icon name="mdi:egg-outline" class="ev-egg-icon" />
                            <Icon name="mdi:water" class="ev-egg-overlay ev-egg-overlay-water" />
                        </div>
                    </div>

                    <div class="ev-tutorial-row ev-tutorial-row-middle">
                        <div class="ev-icon-group ev-ic-tilt-right">
                            <Icon name="mdi:egg-outline" class="ev-egg-icon" />
                            <Icon name="mdi:fire" class="ev-egg-overlay ev-egg-overlay-fire" />
                        </div>
                        <Icon name="mdi:flash" class="ev-flash-icon" />
                        <div class="ev-icon-group ev-ic-tilt-left">
                            <Icon name="mdi:egg-outline" class="ev-egg-icon" />
                            <Icon name="mdi:water" class="ev-egg-overlay ev-egg-overlay-water" />
                        </div>
                    </div>

                    <div class="ev-tutorial-row ev-tutorial-row-bottom">
                        <div class="ev-icon-group ev-ic-rotate">
                            <Icon name="mdi:egg-off-outline" class="ev-egg-icon-sm" />
                            <Icon name="mdi:fire" class="ev-egg-overlay-sm ev-egg-overlay-fire" />
                        </div>
                        <div class="ev-icon-group ev-ic-scale">
                            <Icon name="mdi:egg-outline" class="ev-egg-icon-lg" />
                            <Icon name="mdi:water" class="ev-egg-overlay-lg ev-egg-overlay-water" />
                            <div class="ev-egg-badge">
                                <Icon name="mdi:numeric-2" class="ev-egg-badge-icon" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div id="entities-layer" class="ev-entities-layer"></div>
        </main>

        <div id="encyclopedia-modal" class="ev-modal">
            <div class="ev-modal-content">
                <div class="ev-modal-header">
                    <div class="ev-modal-header-left">
                        <div class="ev-modal-header-icon-box">
                            <Icon name="mdi:book-open-variant" class="ev-modal-header-icon" />
                        </div>
                        <div>
                            <h2 class="ev-modal-title">Enciclopedia</h2>
                            <p class="ev-modal-stats" id="encyclopedia-stats">Descubiertos: 0/0</p>
                        </div>
                    </div>
                    <button id="close-encyclopedia" class="ev-modal-close-btn">
                        <Icon name="mdi:close" class="ev-modal-close-icon" />
                    </button>
                </div>

                <div id="encyclopedia-grid" class="ev-modal-grid"></div>
            </div>
        </div>

        <div id="battle-overlay" class="ev-battle-overlay">
            <div class="ev-battle-container">
                <div class="ev-battle-title-section">
                    <h2 class="ev-battle-title">ARENA DE COMBATE</h2>
                </div>

                <div class="ev-battle-fighters">
                    <div class="ev-fighter" id="fighter-1">
                        <div class="ev-fighter-portrait">
                            <div class="ev-fighter-glow ev-fighter-glow-blue"></div>
                            <div id="f1-visual" class="ev-fighter-visual ev-fighter-visual-blue"></div>
                        </div>
                        <h3 id="f1-name" class="ev-fighter-name">Creature 1</h3>
                        <div class="ev-fighter-hp-track">
                            <div id="f1-hp-bar" class="ev-fighter-hp-fill ev-hp-green"></div>
                        </div>
                        <p id="f1-hp-text" class="ev-fighter-hp-text">100/100</p>
                    </div>

                    <div class="ev-vs-section">
                        <div class="ev-vs-text">VS</div>
                    </div>

                    <div class="ev-fighter" id="fighter-2">
                        <div class="ev-fighter-portrait">
                            <div class="ev-fighter-glow ev-fighter-glow-red"></div>
                            <div id="f2-visual" class="ev-fighter-visual ev-fighter-visual-red"></div>
                        </div>
                        <h3 id="f2-name" class="ev-fighter-name">Creature 2</h3>
                        <div class="ev-fighter-hp-track">
                            <div id="f2-hp-bar" class="ev-fighter-hp-fill ev-hp-red"></div>
                        </div>
                        <p id="f2-hp-text" class="ev-fighter-hp-text">100/100</p>
                    </div>
                </div>

                <div class="ev-battle-action">
                    <p id="battle-action-text" class="ev-battle-action-text"></p>
                </div>

                <button id="close-battle" class="ev-close-battle-btn">
                    Cerrar Arena
                </button>
            </div>
        </div>
    </div>
</Layout>

<style is:global>
    .ev-body {
        background: #020617;
        color: #fff;
        overflow: hidden;
    }
    .ev-main {
        padding: 0 !important;
    }
    .ev-header {
        color: #fff;
        mix-blend-mode: difference;
    }

    .ev-bg-layer {
        position: absolute;
        inset: 0;
        z-index: 0;
        overflow: hidden;
        pointer-events: none;
    }
    .ev-bg-gradient {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: radial-gradient(ellipse at center, #0f172a, #0b0f19, #000);
    }
    .ev-blob {
        position: absolute;
        border-radius: 9999px;
        filter: blur(120px);
        mix-blend-mode: screen;
    }
    .ev-blob-1 {
        top: -20%;
        left: 20%;
        width: 600px;
        height: 600px;
        background: rgba(79, 70, 229, 0.2);
    }
    .ev-blob-2 {
        bottom: -10%;
        right: 10%;
        width: 500px;
        height: 500px;
        background: rgba(192, 38, 211, 0.1);
        filter: blur(100px);
    }
    .ev-grid-pattern {
        position: absolute;
        inset: 0;
        background-image:
            linear-gradient(to right, rgba(255, 255, 255, 0.02) 1px, transparent 1px),
            linear-gradient(to bottom, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
        background-size: 40px 40px;
        -webkit-mask-image: radial-gradient(ellipse at center, black 60%, transparent 100%);
        mask-image: radial-gradient(ellipse at center, black 60%, transparent 100%);
    }

    .ev-layout {
        position: relative;
        z-index: 10;
        display: flex;
        height: 100vh;
        padding-top: 3.75rem;
        overflow: hidden;
        font-family: ui-sans-serif, system-ui, sans-serif;
    }

    .ev-sidebar {
        width: 5rem;
        background: linear-gradient(to bottom, rgba(15, 23, 42, 0.95), rgba(2, 6, 23, 0.95));
        backdrop-filter: blur(24px);
        border-right: 1px solid rgba(255, 255, 255, 0.05);
        display: flex;
        flex-direction: column;
        position: relative;
        overflow: hidden;
    }
    @media (min-width: 768px) {
        .ev-sidebar {
            width: 18rem;
        }
    }
    .ev-sidebar-glow {
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, rgba(99, 102, 241, 0.05), transparent, rgba(168, 85, 247, 0.05));
        pointer-events: none;
    }

    .ev-sidebar-header {
        position: relative;
        padding: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    @media (min-width: 768px) {
        .ev-sidebar-header {
            padding: 1.5rem;
        }
    }
    .ev-sidebar-title {
        display: none;
        font-size: 1.25rem;
        font-weight: 900;
        color: #fff;
        letter-spacing: -0.025em;
        margin-bottom: 0.25rem;
    }
    @media (min-width: 768px) {
        .ev-sidebar-title {
            display: block;
        }
    }
    .ev-sidebar-sub {
        display: none;
        font-size: 0.75rem;
        color: #94a3b8;
        font-family: ui-monospace, monospace;
    }
    @media (min-width: 768px) {
        .ev-sidebar-sub {
            display: block;
        }
    }
    .ev-sidebar-mobile-icon {
        display: flex;
        align-items: center;
        justify-content: center;
    }
    @media (min-width: 768px) {
        .ev-sidebar-mobile-icon {
            display: none;
        }
    }
    .ev-sidebar-mobile-svg {
        width: 2rem;
        height: 2rem;
        color: #818cf8;
    }

    .ev-sidebar-body {
        position: relative;
        padding: 0.75rem;
    }
    @media (min-width: 768px) {
        .ev-sidebar-body {
            padding: 1rem;
        }
    }
    .ev-spawn-btn {
        width: 100%;
        padding: 1rem 0;
        border-radius: 0.75rem;
        background: linear-gradient(135deg, #4f46e5, #9333ea);
        color: #fff;
        font-weight: 700;
        box-shadow: 0 10px 15px -3px rgba(99, 102, 241, 0.3);
        transition: all 0.3s;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.75rem;
        position: relative;
        overflow: hidden;
        border: none;
        cursor: pointer;
    }
    .ev-spawn-btn:hover {
        background: linear-gradient(135deg, #6366f1, #a855f7);
    }
    .ev-spawn-btn:active {
        transform: scale(0.95);
    }
    @media (min-width: 768px) {
        .ev-spawn-btn {
            padding: 0.75rem 0;
        }
    }
    .ev-spawn-btn-overlay {
        position: absolute;
        inset: 0;
        background: rgba(255, 255, 255, 0.1);
        transform: translateY(100%);
        transition: transform 0.3s;
    }
    .ev-spawn-btn:hover .ev-spawn-btn-overlay {
        transform: translateY(0);
    }
    .ev-spawn-btn-icon {
        width: 1.75rem;
        height: 1.75rem;
        transition: transform 0.3s;
        position: relative;
        z-index: 10;
    }
    .ev-spawn-btn:hover .ev-spawn-btn-icon {
        transform: rotate(12deg);
    }
    @media (min-width: 768px) {
        .ev-spawn-btn-icon {
            width: 1.5rem;
            height: 1.5rem;
        }
    }
    .ev-spawn-btn-text {
        display: none;
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        position: relative;
        z-index: 10;
    }
    @media (min-width: 768px) {
        .ev-spawn-btn-text {
            display: inline;
        }
    }

    .ev-divider-section {
        position: relative;
        padding: 0.5rem 0.75rem 0;
    }
    @media (min-width: 768px) {
        .ev-divider-section {
            padding: 0.5rem 1rem 0;
        }
    }
    .ev-divider {
        display: flex;
        align-items: center;
        margin-bottom: 0.75rem;
    }
    .ev-divider-line {
        height: 1px;
        flex: 1;
        background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.2), transparent);
    }
    .ev-divider-text {
        font-size: 0.625rem;
        font-family: ui-monospace, monospace;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        padding: 0 0.5rem;
    }
    @media (min-width: 768px) {
        .ev-divider-text {
            font-size: 0.75rem;
        }
    }

    .ev-inventory {
        position: relative;
        flex: 1;
        overflow-y: auto;
        padding: 0 0.5rem 1rem;
    }
    @media (min-width: 768px) {
        .ev-inventory {
            padding: 0 1rem 1rem;
        }
    }
    .ev-inventory > * + * {
        margin-top: 0.5rem;
    }
    .ev-inventory::-webkit-scrollbar {
        width: 4px;
    }
    .ev-inventory::-webkit-scrollbar-track {
        background: transparent;
    }
    .ev-inventory::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 2px;
    }

    .ev-sidebar-footer {
        position: relative;
        padding: 0.75rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(2, 6, 23, 0.5);
    }
    .ev-sidebar-footer > * + * {
        margin-top: 0.5rem;
    }
    @media (min-width: 768px) {
        .ev-sidebar-footer {
            padding: 1rem;
        }
    }
    .ev-btn {
        width: 100%;
        padding: 0.75rem 0;
        border-radius: 0.5rem;
        font-size: 0.75rem;
        font-weight: 500;
        transition: all 0.3s;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        border: 1px solid;
        cursor: pointer;
    }
    @media (min-width: 768px) {
        .ev-btn {
            padding: 0.625rem 0;
            font-size: 0.875rem;
        }
    }
    .ev-btn-encyclopedia {
        background: rgba(99, 102, 241, 0.1);
        border-color: rgba(99, 102, 241, 0.2);
        color: #818cf8;
    }
    .ev-btn-encyclopedia:hover {
        background: rgba(99, 102, 241, 0.2);
        border-color: rgba(99, 102, 241, 0.3);
        color: #a5b4fc;
    }
    .ev-btn-clear {
        background: rgba(239, 68, 68, 0.1);
        border-color: rgba(239, 68, 68, 0.2);
        color: #f87171;
    }
    .ev-btn-clear:hover {
        background: rgba(239, 68, 68, 0.2);
        border-color: rgba(239, 68, 68, 0.3);
        color: #fca5a5;
    }
    .ev-btn-icon {
        width: 1.25rem;
        height: 1.25rem;
        transition: transform 0.3s;
    }
    .ev-btn:hover .ev-btn-icon {
        transform: scale(1.1);
    }
    @media (min-width: 768px) {
        .ev-btn-icon {
            width: 1rem;
            height: 1rem;
        }
    }
    .ev-btn-text {
        display: none;
    }
    @media (min-width: 768px) {
        .ev-btn-text {
            display: inline;
        }
    }

    .ev-playground {
        flex: 1;
        position: relative;
        overflow: hidden;
        cursor: crosshair;
        background: #44403c;
    }
    .ev-playground-texture {
        position: absolute;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        opacity: 0.2;
        mix-blend-mode: soft-light;
        background-image: url('/assets/textures/stone_wall.webp');
        background-size: cover;
        background-position: center;
    }
    .ev-entities-layer {
        position: absolute;
        inset: 0;
        z-index: 10;
    }

    .ev-tutorial {
        position: absolute;
        inset: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        pointer-events: none;
        z-index: 0;
        -webkit-user-select: none;
        user-select: none;
        opacity: 0.6;
    }
    .ev-tutorial-inner {
        display: flex;
        flex-direction: column;
        gap: 3rem;
        align-items: center;
    }
    .ev-tutorial-row {
        display: flex;
        gap: 8rem;
        align-items: center;
    }
    .ev-tutorial-row-top {
        opacity: 0.8;
    }
    .ev-tutorial-row-middle {
        gap: 4rem;
        opacity: 0.9;
    }
    .ev-tutorial-row-bottom {
        gap: 6rem;
        align-items: flex-end;
        opacity: 0.8;
    }

    .ev-icon-group {
        position: relative;
    }
    .ev-ic-tilt-right {
        transform: rotate(12deg);
        z-index: 10;
    }
    .ev-ic-tilt-left {
        transform: rotate(-12deg);
        z-index: 20;
    }
    .ev-ic-rotate {
        transform: rotate(45deg);
        opacity: 0.6;
    }
    .ev-ic-scale {
        transform: scale(1.1);
    }

    .ev-egg-icon {
        width: 6rem;
        height: 6rem;
        color: #44403c;
    }
    .ev-egg-icon-sm {
        width: 5rem;
        height: 5rem;
        color: #44403c;
    }
    .ev-egg-icon-lg {
        width: 7rem;
        height: 7rem;
        color: #44403c;
    }
    .ev-egg-overlay {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 2.5rem;
        height: 2.5rem;
    }
    .ev-egg-overlay-fire {
        color: rgba(194, 65, 12, 0.7);
    }
    .ev-egg-overlay-water {
        color: rgba(37, 99, 235, 0.7);
    }
    .ev-egg-overlay-sm {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 2rem;
        height: 2rem;
    }
    .ev-egg-overlay-lg {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 3rem;
        height: 3rem;
    }
    .ev-flash-icon {
        width: 4rem;
        height: 4rem;
        color: #d6d3d1;
        z-index: 30;
        margin-top: -2rem;
    }
    .ev-egg-badge {
        position: absolute;
        top: -0.5rem;
        right: -0.5rem;
        background: #44403c;
        border-radius: 9999px;
        padding: 0.25rem;
        border: 1px solid #57534e;
    }
    .ev-egg-badge-icon {
        width: 1.5rem;
        height: 1.5rem;
        color: #a8a29e;
    }

    .ev-modal {
        position: fixed;
        inset: 0;
        z-index: 50;
        background: rgba(2, 6, 23, 0.95);
        backdrop-filter: blur(24px);
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        pointer-events: none;
        transition: all 0.3s;
        padding: 1rem;
    }
    .ev-modal-content {
        width: 100%;
        max-width: 56rem;
        background: #0f172a;
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1rem;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
        display: flex;
        flex-direction: column;
        max-height: 85vh;
    }
    .ev-modal-header {
        padding: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: rgba(15, 23, 42, 0.5);
        border-radius: 1rem 1rem 0 0;
    }
    .ev-modal-header-left {
        display: flex;
        align-items: center;
        gap: 1rem;
    }
    .ev-modal-header-icon-box {
        width: 3rem;
        height: 3rem;
        border-radius: 0.75rem;
        background: rgba(99, 102, 241, 0.1);
        display: flex;
        align-items: center;
        justify-content: center;
        border: 1px solid rgba(99, 102, 241, 0.2);
    }
    .ev-modal-header-icon {
        width: 1.5rem;
        height: 1.5rem;
        color: #818cf8;
    }
    .ev-modal-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: #fff;
    }
    .ev-modal-stats {
        font-size: 0.875rem;
        color: #94a3b8;
        font-family: ui-monospace, monospace;
    }
    .ev-modal-close-btn {
        padding: 0.5rem;
        background: none;
        border: none;
        border-radius: 0.5rem;
        color: #94a3b8;
        cursor: pointer;
        transition: all 0.3s;
    }
    .ev-modal-close-btn:hover {
        background: rgba(255, 255, 255, 0.1);
        color: #fff;
    }
    .ev-modal-close-icon {
        width: 1.5rem;
        height: 1.5rem;
    }
    .ev-modal-grid {
        padding: 1.5rem;
        overflow-y: auto;
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    @media (min-width: 768px) {
        .ev-modal-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }
    @media (min-width: 1024px) {
        .ev-modal-grid {
            grid-template-columns: repeat(3, 1fr);
        }
    }
    .ev-modal-grid::-webkit-scrollbar {
        width: 4px;
    }
    .ev-modal-grid::-webkit-scrollbar-track {
        background: transparent;
    }
    .ev-modal-grid::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 2px;
    }

    .ev-battle-overlay {
        position: fixed;
        inset: 0;
        z-index: 50;
        background: rgba(2, 6, 23, 0.95);
        backdrop-filter: blur(24px);
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        pointer-events: none;
        transition: all 0.5s;
        padding: 1rem;
    }
    .ev-battle-overlay--active {
        opacity: 1 !important;
        pointer-events: auto !important;
    }
    .evo-fighter-icon {
        width: 100%;
        height: 100%;
    }
    .ev-battle-container {
        width: 100%;
        max-width: 72rem;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
    }
    .ev-battle-title-section {
        margin-bottom: 0.75rem;
        text-align: center;
    }
    @media (min-width: 768px) {
        .ev-battle-title-section {
            margin-bottom: 2rem;
        }
    }
    .ev-battle-title {
        font-size: 1.5rem;
        font-weight: 900;
        font-style: italic;
        color: #fff;
        letter-spacing: -0.05em;
        filter: drop-shadow(0 0 15px rgba(255, 255, 255, 0.5));
    }
    @media (min-width: 768px) {
        .ev-battle-title {
            font-size: 2.25rem;
        }
    }

    .ev-battle-fighters {
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-between;
        padding: 0 1rem;
        gap: 1rem;
    }
    @media (min-width: 768px) {
        .ev-battle-fighters {
            flex-direction: row;
            padding: 0 5rem;
            gap: 2rem;
        }
    }

    .ev-fighter {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
    }
    @media (min-width: 768px) {
        .ev-fighter {
            width: auto;
        }
    }

    .ev-fighter-portrait {
        position: relative;
        width: 5rem;
        height: 5rem;
        margin-bottom: 0.5rem;
    }
    @media (min-width: 768px) {
        .ev-fighter-portrait {
            width: 14rem;
            height: 14rem;
            margin-bottom: 1.5rem;
        }
    }

    .ev-fighter-glow {
        position: absolute;
        inset: 0;
        border-radius: 9999px;
        filter: blur(50px);
        animation: ev-pulse 2s infinite;
    }
    .ev-fighter-glow-blue {
        background: rgba(99, 102, 241, 0.2);
    }
    .ev-fighter-glow-red {
        background: rgba(239, 68, 68, 0.2);
    }
    @keyframes ev-pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }

    .ev-fighter-visual {
        position: relative;
        z-index: 10;
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.1s;
    }
    .ev-fighter-visual-blue {
        color: #a5b4fc;
    }
    .ev-fighter-visual-red {
        color: #fca5a5;
    }

    .ev-fighter-name {
        font-size: 1rem;
        font-weight: 700;
        color: #fff;
        margin-bottom: 0.25rem;
    }
    @media (min-width: 768px) {
        .ev-fighter-name {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }
    }

    .ev-fighter-hp-track {
        width: 100%;
        max-width: 150px;
        height: 0.5rem;
        background: #1e293b;
        border-radius: 9999px;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }
    @media (min-width: 768px) {
        .ev-fighter-hp-track {
            max-width: 200px;
            height: 1rem;
        }
    }
    .ev-fighter-hp-fill {
        height: 100%;
        transition: width 0.2s;
    }
    .ev-hp-green {
        background: linear-gradient(to right, #10b981, #34d399);
    }
    .ev-hp-red {
        background: linear-gradient(to right, #ef4444, #f87171);
    }

    .ev-fighter-hp-text {
        font-family: ui-monospace, monospace;
        font-size: 0.75rem;
        color: #94a3b8;
        margin-top: 0.25rem;
    }

    .ev-vs-section {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0.5rem 0;
    }
    @media (min-width: 768px) {
        .ev-vs-section {
            padding: 0;
        }
    }
    .ev-vs-text {
        font-size: 1.875rem;
        font-weight: 900;
        color: #ef4444;
        font-style: italic;
        animation: ev-pulse 2s infinite;
        filter: drop-shadow(0 10px 8px rgba(0, 0, 0, 0.3));
    }
    @media (min-width: 768px) {
        .ev-vs-text {
            font-size: 4.5rem;
        }
    }

    .ev-battle-action {
        margin-top: 0.75rem;
        height: 3rem;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0 1rem;
    }
    @media (min-width: 768px) {
        .ev-battle-action {
            margin-top: 2rem;
            height: 5rem;
        }
    }
    .ev-battle-action-text {
        font-size: 0.875rem;
        font-family: ui-monospace, monospace;
        font-weight: 700;
        color: rgba(255, 255, 255, 0.8);
        text-align: center;
        animation: ev-bounce 1s infinite;
    }
    @media (min-width: 768px) {
        .ev-battle-action-text {
            font-size: 1.5rem;
        }
    }
    @keyframes ev-bounce {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }

    .ev-close-battle-btn {
        margin-top: 0.75rem;
        padding: 0.625rem 1.5rem;
        background: #fff;
        color: #0f172a;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        border-radius: 9999px;
        transition: all 0.3s;
        box-shadow: 0 0 30px rgba(255, 255, 255, 0.4);
        display: none;
        font-size: 0.75rem;
        border: none;
        cursor: pointer;
    }
    .ev-close-battle-btn:hover {
        transform: scale(1.05);
    }
    .ev-close-battle-btn:active {
        transform: scale(0.95);
    }
    @media (min-width: 768px) {
        .ev-close-battle-btn {
            margin-top: 1.5rem;
            padding: 1rem 3rem;
            font-size: 1rem;
        }
    }

    .scrollbar-thin::-webkit-scrollbar {
        width: 4px;
    }
    .scrollbar-thin::-webkit-scrollbar-track {
        background: transparent;
    }
    .scrollbar-thin::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 2px;
    }

    @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }
    .animate-float {
        animation: float 4s ease-in-out infinite;
    }

    .hit-shake {
        animation: hit-shake 0.4s cubic-bezier(0.36, 0.07, 0.19, 0.97) both;
    }
    @keyframes hit-shake {
        10%, 90% { transform: translate3d(-2px, 0, 0) rotate(-2deg); }
        20%, 80% { transform: translate3d(4px, 0, 0) rotate(4deg); }
        30%, 50%, 70% { transform: translate3d(-8px, 0, 0) rotate(-8deg); }
        40%, 60% { transform: translate3d(8px, 0, 0) rotate(8deg); }
    }

    .attack-lunge-right {
        animation: lunge-right 0.3s ease-in-out;
    }
    @keyframes lunge-right {
        0% { transform: translateX(0); }
        50% { transform: translateX(100px) scale(1.2); }
        100% { transform: translateX(0); }
    }

    .attack-lunge-left {
        animation: lunge-left 0.3s ease-in-out;
    }
    @keyframes lunge-left {
        0% { transform: translateX(0); }
        50% { transform: translateX(-100px) scale(1.2); }
        100% { transform: translateX(0); }
    }

    @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }
    .animate-float {
        animation: float 4s ease-in-out infinite;
    }

    .hit-shake {
        animation: hit-shake 0.4s cubic-bezier(0.36, 0.07, 0.19, 0.97) both;
    }
    @keyframes hit-shake {
        10%, 90% { transform: translate3d(-2px, 0, 0) rotate(-2deg); }
        20%, 80% { transform: translate3d(4px, 0, 0) rotate(4deg); }
        30%, 50%, 70% { transform: translate3d(-8px, 0, 0) rotate(-8deg); }
        40%, 60% { transform: translate3d(8px, 0, 0) rotate(8deg); }
    }

    .attack-lunge-right {
        animation: lunge-right 0.3s ease-in-out;
    }
    @keyframes lunge-right {
        0% { transform: translateX(0); }
        50% { transform: translateX(100px) scale(1.2); }
        100% { transform: translateX(0); }
    }

    .attack-lunge-left {
        animation: lunge-left 0.3s ease-in-out;
    }
    @keyframes lunge-left {
        0% { transform: translateX(0); }
        50% { transform: translateX(-100px) scale(1.2); }
        100% { transform: translateX(0); }
    }

    .carved-icon,
    .carved-text {
        filter: drop-shadow(1px 1px 0px rgba(255, 255, 255, 0.3));
    }

    .evo-inventory-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem;
        border-radius: 0.5rem;
        background-color: rgba(30, 41, 59, 0.5);
        border: 1px solid rgba(255, 255, 255, 0.05);
        transition: all 0.2s;
        cursor: grab;
        user-select: none;
        touch-action: manipulation;
    }
    .evo-inventory-item:hover {
        background-color: rgba(79, 70, 229, 0.2);
        border-color: rgba(99, 102, 241, 0.3);
    }
    .evo-inventory-item:active {
        cursor: grabbing;
    }
    .evo-inventory-icon-box {
        width: 2.5rem;
        height: 2.5rem;
        border-radius: 0.25rem;
        background-color: #0f172a;
        display: flex;
        align-items: center;
        justify-content: center;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }
    @media (max-width: 767px) {
        .evo-inventory-item {
            padding: 0.5rem;
        }
        .evo-inventory-icon-box {
            width: 2rem;
            height: 2rem;
        }
    }
    .evo-inventory-icon {
        color: #cbd5e1;
        font-size: 1.25rem;
        transition: color 0.2s;
    }
    .evo-inventory-item:hover .evo-inventory-icon {
        color: #a5b4fc;
    }
    .evo-inventory-name-box {
        flex: 1;
        min-width: 0;
        display: none;
    }
    @media (min-width: 768px) {
        .evo-inventory-name-box {
            display: block;
        }
    }
    .evo-inventory-name {
        font-size: 0.75rem;
        font-weight: bold;
        color: #cbd5e1;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        margin: 0;
    }
    .evo-inventory-item:hover .evo-inventory-name {
        color: #ffffff;
    }
    .evo-entity {
        position: absolute;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 6rem;
        height: 6rem;
        cursor: grab;
        transition: transform 0.2s;
        z-index: 10;
        touch-action: none;
    }
    .evo-entity--dragging {
        opacity: 0.5;
    }
    .evo-drag-ghost {
        position: fixed;
        z-index: 50;
        width: 3rem;
        height: 3rem;
        background-color: #4f46e5;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #ffffff;
        pointer-events: none;
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        opacity: 0.8;
    }
    .evo-drag-ghost-icon {
        font-size: 1.5rem;
    }
    .evo-fx-particle {
        position: absolute;
        width: 0.5rem;
        height: 0.5rem;
        border-radius: 50%;
        pointer-events: none;
        z-index: 50;
    }
    .evo-entity:active {
        cursor: grabbing;
    }
    .evo-entity:hover {
        transform: translate(-50%, -50%) scale(1.1) !important;
    }
    .evo-entity--feeding {
        transform: translate(-50%, -50%) scale(1.25) !important;
    }
    .evo-entity-visual {
        position: relative;
        width: 4rem;
        height: 4rem;
        border-radius: 50%;
        border: 1px solid rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(4px);
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        pointer-events: none;
    }
    .evo-entity-icon {
        font-size: 2.25rem;
    }
    .evo-entity-name {
        position: absolute;
        bottom: -0.5rem;
        padding: 0.125rem 0.5rem;
        background-color: rgba(0, 0, 0, 0.8);
        border-radius: 9999px;
        font-size: 9px;
        font-weight: bold;
        color: #ffffff;
        border: 1px solid rgba(255, 255, 255, 0.1);
        white-space: nowrap;
    }
    .evo-type-fuego {
        color: #ffedd5;
        border-color: rgba(255, 87, 34, 0.3);
        background-color: rgba(255, 87, 34, 0.15);
        filter: drop-shadow(0 0 15px rgba(255, 87, 34, 0.5));
    }
    .evo-type-agua {
        color: #bfdbfe;
        border-color: rgba(33, 150, 243, 0.3);
        background-color: rgba(33, 150, 243, 0.15);
        filter: drop-shadow(0 0 15px rgba(33, 150, 243, 0.5));
    }
    .evo-type-tierra {
        color: #fef3c7;
        border-color: rgba(121, 85, 72, 0.3);
        background-color: rgba(121, 85, 72, 0.15);
        filter: drop-shadow(0 0 15px rgba(121, 85, 72, 0.5));
    }
    .evo-type-aire {
        color: #e2e8f0;
        border-color: rgba(158, 158, 158, 0.3);
        background-color: rgba(158, 158, 158, 0.15);
        filter: drop-shadow(0 0 15px rgba(158, 158, 158, 0.5));
    }
    .evo-type-poop {
        color: #b45309;
        border-color: rgba(180, 83, 9, 0.3);
        background-color: rgba(120, 53, 15, 0.2);
        filter: drop-shadow(0 0 5px rgba(180, 83, 9, 0.3));
    }
    .evo-entity-slots {
        position: absolute;
        top: -1rem;
        display: flex;
        gap: 0.25rem;
    }
    .evo-entity-slot {
        width: 0.75rem;
        height: 0.75rem;
        border-radius: 50%;
        box-sizing: border-box;
    }
    .evo-entity-slot--empty {
        background-color: #1e293b;
        border: 1px dashed rgba(255, 255, 255, 0.2);
    }
    .evo-entity-slot--filled {
        border: 1px solid rgba(255, 255, 255, 0.2);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 8px;
        color: #ffffff;
    }
    .evo-entity-level {
        position: absolute;
        right: -0.5rem;
        top: -0.5rem;
        width: 1.25rem;
        height: 1.25rem;
        border-radius: 50%;
        background-color: #f59e0b;
        color: #0f172a;
        font-size: 10px;
        font-weight: 900;
        display: flex;
        align-items: center;
        justify-content: center;
        border: 1px solid rgba(255, 255, 255, 0.2);
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        z-index: 20;
    }
    .evo-entity-hp-container {
        position: absolute;
        bottom: -1rem;
        width: 4rem;
        height: 6px;
        background-color: rgba(15, 23, 42, 0.8);
        border-radius: 9999px;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }
    .evo-entity-hp-bar {
        height: 100%;
        transition: width 0.3s;
    }
    .evo-hp-bar--ok {
        background-color: #10b981;
    }
    .evo-hp-bar--low {
        background-color: #ef4444;
    }
    .evo-entity-xp-container {
        position: absolute;
        bottom: -1.5rem;
        width: 3.5rem;
        height: 4px;
        background-color: rgba(15, 23, 42, 0.8);
        border-radius: 9999px;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }
    .evo-entity-xp-bar {
        height: 100%;
        background-color: #818cf8;
        transition: width 0.3s;
    }
    .evo-enc-card {
        position: relative;
        padding: 1rem;
        border-radius: 0.75rem;
        border: 1px solid;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        transition: all 0.2s;
    }
    .evo-enc-card:hover {
        transform: scale(1.02);
    }
    .evo-enc-card--discovered {
        background-color: rgba(30, 41, 59, 0.5);
        border-color: rgba(255, 255, 255, 0.1);
    }
    .evo-enc-card--locked {
        background-color: rgba(15, 23, 42, 0.5);
        border-color: rgba(255, 255, 255, 0.05);
    }
    .evo-enc-card-icon-box-wrapper {
        margin-bottom: 0.75rem;
        position: relative;
    }
    .evo-enc-card-icon-box-wrapper.evo-locked {
        opacity: 0.2;
    }
    .evo-enc-card-icon-box {
        width: 4rem;
        height: 4rem;
        border-radius: 50%;
        background-color: #0f172a;
        border: 1px solid rgba(255, 255, 255, 0.1);
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        font-size: 1.875rem;
    }
    .evo-enc-recipe-title {
        font-size: 1.125rem;
        font-weight: bold;
        color: #ffffff;
        margin-bottom: 0.25rem;
        margin-top: 0;
    }
    .evo-locked-title {
        font-size: 1.125rem;
        font-weight: bold;
        color: #334155;
        margin-bottom: 0.25rem;
        margin-top: 0;
    }
    .evo-enc-recipe-desc {
        font-size: 0.75rem;
        color: #94a3b8;
        margin-bottom: 1rem;
        height: 2rem;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
        margin-top: 0;
    }
    .evo-locked-desc {
        font-size: 0.75rem;
        color: #1e293b;
        margin-bottom: 1rem;
        margin-top: 0;
    }
    .evo-enc-recipe-inputs-container {
        width: 100%;
        padding-top: 0.75rem;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
    }
    .evo-enc-recipe-label {
        font-size: 10px;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: #64748b;
        margin-bottom: 0.5rem;
        margin-top: 0;
    }
    .evo-enc-recipe-inputs {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
    }
    .evo-enc-recipe-input {
        width: 2rem;
        height: 2rem;
        border-radius: 0.25rem;
        background-color: #0f172a;
        display: flex;
        align-items: center;
        justify-content: center;
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #94a3b8;
        font-size: 1rem;
    }
    .evo-enc-recipe-plus {
        color: #475569;
        font-size: 0.875rem;
    }
</style>

<script>
    import { Game } from "../../../scripts/evolution/Game.js";

    function initGame() {
        const dataScript = document.getElementById("evolution-data");
        const playground = document.getElementById("playground");

        if (window["__gameInstance"]) {
            try {
                window["__gameInstance"].destroy();
            } catch (e) {
                console.error(e);
            }
        }

        if (dataScript && dataScript.textContent) {
            try {
                const { base_elements, recipes } = JSON.parse(dataScript.textContent);
                window["__gameInstance"] = new Game({ base_elements, recipes });
            } catch (err) {
                console.error("Error initializing Game:", err);
            }
        }
    }

    if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", initGame);
    } else {
        initGame();
    }
    document.addEventListener("astro:page-load", initGame);

    const script = document.createElement("script");
    script.src = "https://code.iconify.design/3/3.1.0/iconify.min.js";
    document.head.appendChild(script);
</script>

<script
    is:inline
    id="evolution-data"
    type="application/json"
    set:html={JSON.stringify({ base_elements, recipes })}
></script>


