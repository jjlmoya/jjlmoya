---
import GameBobLayout from "../../../layouts/GameBobLayout.astro";
import { Icon } from "astro-icon/components";
import { prototypes } from "../../../data/gamebob/prototypes";
---

<GameBobLayout
    title="Prototipos - GameBob"
    description="Zona de pruebas experimental. Prototipos en fase muy temprana (pre-alpha) para validar ideas de juego."
    image="/images/gamebob/prototipos/index.webp"
>
    <div class="min-h-screen flex flex-col items-center py-12 px-4">
        <!-- Header -->
        <div class="z-10 w-full max-w-6xl mt-8 mb-16">
            <h1
                class="text-4xl font-bold mb-4 border-l-4 border-purple-500 pl-4 lowercase text-[var(--color-gamebob-text)]"
            >
                prototipos
            </h1>
            <p class="text-gray-400 text-lg font-light max-w-2xl">
                zona de experimentación radical. ideas en fase muy temprana.
            </p>
        </div>

        <!-- Warning Banner -->
        <div
            class="w-full max-w-3xl mx-auto mb-16 p-6 rounded-xl bg-yellow-500/10 border border-yellow-500/20 flex gap-4 items-start"
        >
            <Icon name="mdi:alert-decagram" class="w-8 h-8 text-yellow-500 flex-shrink-0 mt-1" />
            <div class="space-y-2">
                <h3 class="text-yellow-500 font-bold uppercase tracking-widest text-sm">
                    Advertencia: Zona Inestable
                </h3>
                <p class="text-yellow-200/80 text-sm leading-relaxed">
                    Estos prototipos están tan verdes que todavía hacen la fotosíntesis. Son
                    versiones muy tempranas creadas únicamente para validar si una idea rudimentaria
                    es divertida. Pueden contener errores, romperse sin previo aviso o no estar
                    optimizados. Juega bajo tu propia responsabilidad. Y ni se te ocurre divertirte,
                    esto es trabajo puro y duro.
                </p>
            </div>
        </div>

        <!-- Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 w-full max-w-6xl">
            {
                prototypes.map((proto) => (
                    <a
                        href={`/gamebob/prototipos/${proto.slug}/`}
                        class={`group relative p-8 rounded-2xl bg-slate-900/50 border border-white/5 ${proto.borderColor} transition-all duration-300 hover:-translate-y-1 hover:shadow-2xl ${proto.bgHover}`}
                    >
                        <div class="absolute top-4 right-4 opacity-20 group-hover:opacity-100 transition-opacity">
                            <Icon name="mdi:arrow-top-right" class="w-6 h-6 text-white" />
                        </div>

                        <div class="mb-6 p-4 rounded-xl bg-slate-950 inline-block border border-white/5 group-hover:scale-110 transition-transform duration-300">
                            <Icon name={proto.icon} class={`w-10 h-10 ${proto.color}`} />
                        </div>

                        <h2 class="text-2xl font-bold text-white mb-3 tracking-tight">
                            {proto.title}
                        </h2>
                        <p class="text-gray-400 text-sm leading-relaxed mb-6">
                            {proto.description}
                        </p>

                        {proto.verdict === "liked" && (
                            <div class="flex items-center gap-2 text-xs font-mono uppercase tracking-widest text-emerald-400 opacity-80 group-hover:opacity-100 transition-opacity">
                                <Icon name="mdi:thumb-up" class="w-4 h-4" />
                                Ma gustao
                            </div>
                        )}

                        {proto.verdict === "disliked" && (
                            <div class="flex items-center gap-2 text-xs font-mono uppercase tracking-widest text-red-400 opacity-80 group-hover:opacity-100 transition-opacity">
                                <Icon name="mdi:thumb-down" class="w-4 h-4" />
                                No ma gustao
                            </div>
                        )}
                    </a>
                ))
            }
        </div>

        <!-- Legend -->
        <div class="mt-24 w-full max-w-4xl mx-auto border-t border-white/10 pt-12">
            <h3 class="text-sm font-mono uppercase tracking-widest text-center text-gray-500 mb-8">
                Leyenda de Veredictos
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Liked -->
                <div
                    class="flex gap-4 items-start p-6 rounded-xl bg-emerald-500/5 border border-emerald-500/10"
                >
                    <Icon name="mdi:thumb-up" class="w-5 h-5 text-emerald-400 flex-shrink-0 mt-1" />
                    <div>
                        <div
                            class="font-mono uppercase tracking-widest text-emerald-400 text-xs font-bold mb-2"
                        >
                            Ma gustao
                        </div>
                        <p class="text-gray-400 text-sm leading-relaxed">
                            Posiblemente en algún momento se vuelva un juego más completo dándole
                            una vuelta a las mecánicas y puliéndolo.
                        </p>
                    </div>
                </div>

                <!-- Disliked -->
                <div
                    class="flex gap-4 items-start p-6 rounded-xl bg-red-500/5 border border-red-500/10"
                >
                    <Icon name="mdi:thumb-down" class="w-5 h-5 text-red-400 flex-shrink-0 mt-1" />
                    <div>
                        <div
                            class="font-mono uppercase tracking-widest text-red-400 text-xs font-bold mb-2"
                        >
                            No ma gustao
                        </div>
                        <p class="text-gray-400 text-sm leading-relaxed">
                            La idea resonaba increíble en mi cabeza pero no ha aterrizado. Si sabes
                            cómo arreglarlo, contacta conmigo para ver si salvamos este
                            despropósito.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</GameBobLayout>
