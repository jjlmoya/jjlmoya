---
export const prerender = true;
import Layout from "../../layouts/Layout.astro";
import { sections } from "../../data/utilities/index";
import { getStaticPageAlternates } from "../../i18n/gamebob";
import { getWidgetHtml } from "../../utils/widget";
import WidgetSidebar from "../../components/widgets/WidgetSidebar.astro";
import WidgetHeader from "../../components/widgets/WidgetHeader.astro";
import WidgetPreview from "../../components/widgets/WidgetPreview.astro";
import WidgetEmbedCodes from "../../components/widgets/WidgetEmbedCodes.astro";
import WidgetEducation from "../../components/widgets/WidgetEducation.astro";
import WidgetPlatforms from "../../components/widgets/WidgetPlatforms.astro";
import WidgetWordPressGuide from "../../components/widgets/WidgetWordPressGuide.astro";
import WidgetBenefits from "../../components/widgets/WidgetBenefits.astro";

const sectionsWithHtml = sections.map((section) => ({
    title: section.title,
    utilities: section.utilities.map((utility) => {
        const utilityName = utility.href.split("/").filter(Boolean).pop() || "utility";
        const widgetId = `wj-${utilityName}`;
        const baseUrl = Astro.url.origin;
        const widgetUrl = `${baseUrl}${utility.href}${utility.href.endsWith("/") ? "" : "/"}?widget=true`;
        const cleanUrl = `${baseUrl}${utility.href}${utility.href.endsWith("/") ? "" : "/"}`;

        return {
            ...utility,
            widgetId,
            html: getWidgetHtml({ widgetId, widgetUrl, cleanUrl }),
        };
    }),
}));

const flatUtilities = sectionsWithHtml.flatMap((s) => s.utilities);

const alternateUrls = getStaticPageAlternates("/widgets");

const title = "Estudio de Utilidades: Calculadoras Gratis e Interacción para tu Web";
const description =
    "Potencia tu sitio con herramientas interactivas. Inserta calculadoras de cocina, utilidades técnicas y herramientas personalizadas gratis. Optimizado para WordPress y sitios personalizados.";
const image = "/assets/widgets/hero.webp";
---

<Layout title={title} description={description} image={image} fullWidth={true} alternateUrls={alternateUrls}>
    <script slot="head" is:inline>
        if (window.location.search) {
            const m = document.createElement("meta");
            m.name = "robots";
            m.content = "noindex, follow";
            document.head.appendChild(m);
        }
    </script>
    <div class="wi-layout">
        <WidgetSidebar sections={sectionsWithHtml} />

        <main class="wi-main custom-scrollbar">
            <WidgetHeader />

            <div class="wi-content">
                <div class="wi-content-inner">
                    <WidgetPreview />
                    <WidgetEmbedCodes />

                    <div class="wi-section-group">
                        <WidgetEducation />
                        <WidgetPlatforms />
                        <WidgetWordPressGuide />
                    </div>
                </div>
            </div>

            <WidgetBenefits />
        </main>
    </div>
</Layout>

<script is:inline define:vars={{ flatUtilities }}>
    let activeUtility = null;
    let activeWidgetTheme = "light";

    const searchInput = document.getElementById("widget-search");
    const buttons = document.querySelectorAll(".ws-utility-btn");
    const toggles = document.querySelectorAll(".ws-category-toggle");
    const display = document.getElementById("canvas-inner");
    const canvas = document.getElementById("preview-canvas");
    const titleEl = document.getElementById("current-title");
    const descEl = document.getElementById("current-desc");
    const categoryBadge = document.getElementById("current-category-badge");
    const embedSection = document.getElementById("embed-section");
    const codeIframe = document.getElementById("code-iframe");
    const copyIframeBtn = document.getElementById("copy-iframe");
    const widgetThemeBtns = document.querySelectorAll(".wh-theme-btn");

    function getWidgetHtmlClient({ widgetId, widgetUrl, cleanUrl, theme }) {
        const params = new URLSearchParams();
        if (theme) params.append("theme", theme);
        const queryString = params.toString();
        const finalWidgetUrl = queryString
            ? `${widgetUrl}${widgetUrl.includes("?") ? "&" : "?"}${queryString}`
            : widgetUrl;

        const isDark = theme === "dark";
        const bg = isDark ? "#09090b" : "#fff";
        const border = isDark ? "#27272a" : "#e2e8f0";

        return `\x3Cdiv id="${widgetId}-wrp" style="position: relative; width: 100%; border: 1px solid ${border}; border-radius: 12px; overflow: hidden; background: ${bg};">
    \x3Cscript>
        (function() {
            var id = '${widgetId}';
            function update(h) {
                var f = document.getElementById(id);
                var l = document.getElementById(id + '-loader');
                if (f && h) f.style.height = h + 'px';
                if (f) f.style.opacity = '1';
                if (l) { l.style.opacity = '0'; setTimeout(function(){ l.style.display = 'none'; }, 400); }
            }
            window.addEventListener('message', function(e) {
                if (e.data && e.data.jjlmoyaId === id && e.data.jjlmoyaHeight) {
                    update(e.data.jjlmoyaHeight);
                }
            });
            window._jjshow = update;
        })();
    \x3C/script>
    \x3Cdiv id="${widgetId}-loader" style="position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; background: ${bg}; z-index: 10; transition: opacity 0.4s;">
        \x3Cdiv style="width: 24px; height: 24px; border: 2px solid ${isDark ? "#18181b" : "#f3f4f6"}; border-top-color: #94a3b8; border-radius: 50%; animation: jjspin 0.8s linear infinite;">\x3C/div>
    \x3C/div>
    \x3Ciframe id="${widgetId}" src="${finalWidgetUrl}&id=${widgetId}" width="100%" height="400" frameborder="0" scrolling="no" style="display: block; opacity: 0; transition: opacity 0.4s, height 0.3s ease; border: none;" onload="window._jjshow()">\x3C/iframe>
    \x3Cstyle>@keyframes jjspin { to { transform: rotate(360deg); } } #${widgetId}-link:hover { opacity: 1 !important; color: ${isDark ? "#fff" : "#000"} !important; background: ${isDark ? "#18181b" : "#f8fafc"} !important; border-color: ${border} !important; }\x3C/style>
\x3C/div>
\x3Cdiv style="text-align: center; margin-top: 10px;">
    \x3Ca id="${widgetId}-link" href="${cleanUrl}" target="_blank" title="Usa esta utilidad en tu web" style="display: inline-flex; align-items: center; gap: 6px; font-size: 9px; color: #94a3b8; text-decoration: none; padding: 4px 12px; border-radius: 100px; border: 1px solid ${isDark ? "#18181b" : "#f1f5f9"}; background: ${bg}; transition: all 0.2s ease; opacity: 0.8; box-shadow: 0 1px 2px rgba(0,0,0,0.03);">
        \x3Csvg xmlns="http://www.w3.org/2000/svg" width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">\x3Cpolyline points="16 18 22 12 16 6">\x3C/polyline>\x3Cpolyline points="8 6 2 12 8 18">\x3C/polyline>\x3C/svg>
        \x3Cspan style="font-weight: 600; letter-spacing: 0.02em;">jjlmoya.es\x3C/span>
    \x3C/a>
\x3C/div>`;
    }

    function updateCurrentWidget() {
        if (!activeUtility) return;

        const baseUrl = window.location.origin;
        const widgetUrl = `${baseUrl}${activeUtility.href}${activeUtility.href.endsWith("/") ? "" : "/"}?widget=true`;
        const cleanUrl = `${baseUrl}${activeUtility.href}${activeUtility.href.endsWith("/") ? "" : "/"}`;
        const widgetId = activeUtility.widgetId;

        const html = getWidgetHtmlClient({
            widgetId,
            widgetUrl,
            cleanUrl,
            theme: activeWidgetTheme,
        });

        codeIframe.textContent = html.trim();

        const range = document.createRange();
        const fragment = range.createContextualFragment(html);
        display.innerHTML = "";
        display.appendChild(fragment);
    }

    toggles.forEach((toggle) => {
        toggle.addEventListener("click", () => {
            const group = toggle.closest(".ws-category");
            const content = group.querySelector(".ws-category-content");
            const chevron = toggle.querySelector(".ws-chevron");
            const isHidden = content.classList.contains("ws-category-content--hidden");

            document.querySelectorAll(".ws-category").forEach((otherGroup) => {
                if (otherGroup !== group) {
                    otherGroup.querySelector(".ws-category-content").classList.add("ws-category-content--hidden");
                    otherGroup.querySelector(".ws-chevron").classList.remove("ws-chevron--open");
                    otherGroup.classList.remove("ws-category--open");
                }
            });

            if (isHidden) {
                content.classList.remove("ws-category-content--hidden");
                chevron.classList.add("ws-chevron--open");
                group.classList.add("ws-category--open");
            } else {
                content.classList.add("ws-category-content--hidden");
                chevron.classList.remove("ws-chevron--open");
                group.classList.remove("ws-category--open");
            }
        });
    });

    searchInput?.addEventListener("input", (e) => {
        const term = e.target.value.toLowerCase();

        document.querySelectorAll(".ws-category").forEach((group) => {
            const groupContent = group.querySelector(".ws-category-content");
            const groupToggle = group.querySelector(".ws-category-toggle");
            const groupBtns = group.querySelectorAll(".ws-utility-btn");
            const chevron = groupToggle.querySelector(".ws-chevron");

            let groupHasMatch = false;

            groupBtns.forEach((btn) => {
                const title = btn.dataset.title.toLowerCase();
                const isMatch = title.includes(term);
                btn.style.display = isMatch ? "flex" : "none";
                if (isMatch) groupHasMatch = true;
            });

            group.style.display = groupHasMatch || term === "" ? "block" : "none";

            if (term !== "" && groupHasMatch) {
                groupContent.classList.remove("ws-category-content--hidden");
                chevron.classList.add("ws-chevron--open");
            } else if (term === "") {
                groupContent.classList.add("ws-category-content--hidden");
                chevron.classList.remove("ws-chevron--open");
            }
        });
    });

    document.querySelectorAll(".wh-bg-btn").forEach((btn) => {
        btn.addEventListener("click", () => {
            const bg = btn.dataset.bg;
            document.querySelectorAll(".wh-bg-btn").forEach((b) => b.classList.remove("wh-bg-btn--active"));
            btn.classList.add("wh-bg-btn--active");

            canvas.className = "wprev-canvas";

            if (bg === "white") canvas.classList.add("wi-canvas--white");
            if (bg === "black") canvas.classList.add("wi-canvas--black");
            if (bg === "grid") canvas.classList.add("wi-canvas--grid");
        });
    });

    function setWidgetTheme(theme) {
        activeWidgetTheme = theme;
        widgetThemeBtns.forEach((btn) => {
            if (btn.dataset.theme === theme) {
                btn.classList.add("wh-theme-btn--active");
                btn.classList.remove("wh-theme-btn--inactive");
            } else {
                btn.classList.remove("wh-theme-btn--active");
                btn.classList.add("wh-theme-btn--inactive");
            }
        });
        updateCurrentWidget();
    }

    widgetThemeBtns.forEach((btn) => {
        btn.addEventListener("click", () => setWidgetTheme(btn.dataset.theme));
    });

    window.addEventListener("message", (e) => {
        if (e.data && e.data.jjlmoyaId && e.data.jjlmoyaHeight) {
            const iframe = document.getElementById(e.data.jjlmoyaId);
            if (iframe) {
                iframe.style.height = e.data.jjlmoyaHeight + "px";
                iframe.style.opacity = "1";
            }
            const loader = document.getElementById(e.data.jjlmoyaId + "-loader");
            if (loader) {
                loader.style.opacity = "0";
                setTimeout(() => (loader.style.display = "none"), 400);
            }
        }
    });

    buttons.forEach((btn) => {
        btn.addEventListener("click", () => {
            buttons.forEach((b) => b.classList.remove("active-tool"));
            btn.classList.add("active-tool");

            const href = btn.dataset.href;
            const category = btn.dataset.category;
            const utData = flatUtilities.find((u) => u.href === href);
            if (!utData) return;

            activeUtility = utData;
            activeUtility.category = category;

            const url = new URL(window.location.href);
            const slug = href.split("/").filter(Boolean).pop();
            url.searchParams.set("utilidad", slug);
            window.history.replaceState({}, "", url);

            titleEl.textContent = utData.title;
            categoryBadge.textContent = category;
            categoryBadge.style.opacity = "1";
            descEl.textContent = utData.description || "Widget profesional para tu sitio web.";

            updateCurrentWidget();

            embedSection.classList.add("wec-embed-section--visible");

            if (window.innerWidth < 1024) {
                titleEl.scrollIntoView({ behavior: "smooth", block: "center" });
            }
        });
    });

    function setupCopy(btn, target) {
        btn.addEventListener("click", () => {
            const text = target.textContent;
            navigator.clipboard.writeText(text).then(() => {
                const original = btn.innerHTML;
                btn.innerHTML =
                    '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" class="text-white"><polyline points="20 6 9 17 4 12"></polyline></svg>';
                setTimeout(() => (btn.innerHTML = original), 2000);
            });
        });
    }
    setupCopy(copyIframeBtn, codeIframe);

    window.addEventListener("DOMContentLoaded", () => {
        const urlParams = new URLSearchParams(window.location.search);
        const toolSlug = urlParams.get("utilidad");

        if (toolSlug) {
            const targetBtn = Array.from(buttons).find((btn) =>
                btn.dataset.href.includes(toolSlug)
            );
            if (targetBtn) {
                const group = targetBtn.closest(".ws-category");
                const content = group?.querySelector(".ws-category-content");
                const toggle = group?.querySelector(".ws-category-toggle");

                if (content?.classList.contains("ws-category-content--hidden")) {
                    toggle?.click();
                }

                targetBtn.click();
            }
        }
    });
</script>

<style is:global>
    .wi-layout {
        --wi-bg: #f8fafc;
        --wi-sidebar-bg: #ffffff;
        --wi-sidebar-border: #e2e8f0;
        --wi-sidebar-header-border: #f1f5f9;
        --wi-sidebar-title-color: #4f46e5;
        --wi-subtitle-color: #64748b;
        --wi-search-input-bg: #f1f5f9;
        --wi-category-border: #f1f5f9;
        --wi-category-toggle-bg: rgba(248, 250, 252, 0.5);
        --wi-category-toggle-hover: #f1f5f9;
        --wi-category-label-color: #64748b;
        --wi-category-content-bg: #ffffff;
        --wi-chevron-color: #cbd5e1;
        --wi-utility-btn-hover: #eef2ff;
        --wi-utility-btn-label: #475569;
        --wi-utility-btn-active-color: #4f46e5;
        --wi-utility-btn-active-border: #4f46e5;
        --wi-utility-btn-active-bg: rgba(79, 70, 229, 0.05);
        --wi-utility-btn-active-bg-lg: rgba(79, 70, 229, 0.08);
        --wi-suggest-bg: #f8fafc;
        --wi-suggest-border: #f1f5f9;
        --wi-suggest-icon-wrapper: #eef2ff;
        --wi-suggest-icon-color: #4f46e5;
        --wi-suggest-title: #0f172a;
        --wi-suggest-desc: #64748b;

        --wi-header-bg: rgba(248, 250, 252, 0.9);
        --wi-header-border: #e2e8f0;
        --wi-badge-bg: #eef2ff;
        --wi-badge-color: #4f46e5;
        --wi-title-color: #0f172a;
        --wi-desc-color: #64748b;
        --wi-control-group-bg: #ffffff;
        --wi-control-group-border: #e2e8f0;
        --wi-theme-btn-hover: #f8fafc;
        --wi-theme-btn-active-bg: #eef2ff;
        --wi-theme-btn-active-color: #4f46e5;
        --wi-theme-btn-inactive-color: #94a3b8;
        --wi-preview-border: #e2e8f0;
        --wi-empty-icon-wrapper: #eef2ff;
        --wi-empty-text: #94a3b8;

        --wi-card-bg: #ffffff;
        --wi-card-border: #e2e8f0;
        --wi-card-title-color: #4f46e5;
        --wi-card-icon-color: #cbd5e1;
        --wi-card-desc-color: #64748b;
        --wi-code-bg: #f8fafc;
        --wi-code-color: #475569;
        --wi-code-border: #f1f5f9;
        --wi-fade-from: #f8fafc;

        --wi-education-indigo-bg: #eef2ff;
        --wi-education-indigo-border: #e0e7ff;
        --wi-education-slate-bg: #f8fafc;
        --wi-education-slate-border: #f1f5f9;
        --wi-education-title-color: #0f172a;
        --wi-education-text-color: #475569;
        --wi-tag-green-bg: #dcfce7;
        --wi-tag-green-color: #166534;
        --wi-tag-indigo-bg: #e0e7ff;
        --wi-tag-indigo-color: #3730a3;
        --wi-tag-blue-bg: #dbeafe;
        --wi-tag-blue-color: #1e40af;
        --wi-tag-slate-bg: #e2e8f0;
        --wi-tag-slate-color: #1e293b;

        --wi-platform-section-border: #f1f5f9;
        --wi-platform-card-bg: #ffffff;
        --wi-platform-card-border: #e2e8f0;
        --wi-platform-header-color: #0f172a;
        --wi-platform-text-color: #64748b;
        --wi-platform-icon-color: #000000;
        --wi-platform-icon-color--notion: #0f172a;
        --wi-platform-icon-color--square: #000000;

        --wi-wp-guide-bg: #f8fafc;
        --wi-wp-guide-border: #f1f5f9;
        --wi-wp-badge-a-bg: #e2e8f0;
        --wi-wp-badge-b-bg: #e0e7ff;
        --wi-wp-badge-b-color: #4338ca;
        --wi-wp-option-title-color: #0f172a;
        --wi-wp-text-color: #64748b;
        --wi-wp-step-color: #475569;
        --wi-wp-alert-amber-bg: #fffbeb;
        --wi-wp-alert-amber-color: #92400e;
        --wi-wp-alert-amber-border: #fef3c7;
        --wi-wp-alert-green-bg: #ecfdf5;
        --wi-wp-alert-green-color: #064e3b;
        --wi-wp-alert-green-border: #d1fae5;
        --wi-wp-alert-icon-emerald: #059669;

        --wi-benefits-bg: rgba(248, 250, 252, 0.5);
        --wi-benefits-border: #e2e8f0;
        --wi-benefits-title: #0f172a;
        --wi-benefits-desc: #64748b;
        --wi-benefits-icon-wrapper: #eef2ff;
        --wi-benefits-icon-color: #4f46e5;
        --wi-benefits-item-title: #0f172a;
        --wi-benefits-item-desc: #64748b;
        --wi-benefits-article-title: #0f172a;
        --wi-benefits-platform-card-bg: #ffffff;
        --wi-benefits-platform-card-border: #e2e8f0;
        --wi-benefits-platform-name: #334155;
    }

    .theme-dark .wi-layout {
        --wi-bg: #000000;
        --wi-sidebar-bg: #09090b;
        --wi-sidebar-border: #1e293b;
        --wi-sidebar-header-border: #0f172a;
        --wi-sidebar-title-color: #818cf8;
        --wi-subtitle-color: #94a3b8;
        --wi-search-input-bg: #0f172a;
        --wi-category-border: rgba(30, 41, 59, 0.5);
        --wi-category-toggle-bg: rgba(24, 24, 27, 0.5);
        --wi-category-toggle-hover: #18181b;
        --wi-category-label-color: #94a3b8;
        --wi-category-content-bg: #09090b;
        --wi-chevron-color: #94a3b8;
        --wi-utility-btn-hover: rgba(49, 46, 129, 0.1);
        --wi-utility-btn-label: #94a3b8;
        --wi-utility-btn-active-color: #818cf8;
        --wi-utility-btn-active-border: #818cf8;
        --wi-utility-btn-active-bg: rgba(129, 140, 248, 0.08);
        --wi-utility-btn-active-bg-lg: rgba(129, 140, 248, 0.12);
        --wi-suggest-bg: rgba(24, 24, 27, 0.5);
        --wi-suggest-border: rgba(30, 41, 59, 0.5);
        --wi-suggest-icon-wrapper: rgba(49, 46, 129, 0.3);
        --wi-suggest-icon-color: #818cf8;
        --wi-suggest-title: #ffffff;
        --wi-suggest-desc: #94a3b8;

        --wi-header-bg: rgba(0, 0, 0, 0.9);
        --wi-header-border: #1e293b;
        --wi-badge-bg: rgba(49, 46, 129, 0.2);
        --wi-badge-color: #818cf8;
        --wi-title-color: #ffffff;
        --wi-desc-color: #94a3b8;
        --wi-control-group-bg: #18181b;
        --wi-control-group-border: #1e293b;
        --wi-theme-btn-hover: #27272a;
        --wi-theme-btn-active-bg: #27272a;
        --wi-theme-btn-active-color: #94a3b8;
        --wi-theme-btn-inactive-color: #94a3b8;
        --wi-preview-border: #1e293b;
        --wi-empty-icon-wrapper: rgba(30, 27, 75, 0.3);
        --wi-empty-text: #94a3b8;

        --wi-card-bg: #09090b;
        --wi-card-border: #1e293b;
        --wi-card-title-color: #818cf8;
        --wi-card-icon-color: #1e293b;
        --wi-card-desc-color: #94a3b8;
        --wi-code-bg: #000000;
        --wi-code-color: #94a3b8;
        --wi-code-border: #0f172a;
        --wi-fade-from: #000000;

        --wi-education-indigo-bg: rgba(49, 46, 129, 0.1);
        --wi-education-indigo-border: rgba(55, 48, 163, 0.3);
        --wi-education-slate-bg: rgba(15, 23, 42, 0.5);
        --wi-education-slate-border: #1e293b;
        --wi-education-title-color: #ffffff;
        --wi-education-text-color: #94a3b8;
        --wi-tag-green-bg: rgba(20, 83, 45, 0.3);
        --wi-tag-green-color: #4ade80;
        --wi-tag-indigo-bg: rgba(49, 46, 129, 0.3);
        --wi-tag-indigo-color: #818cf8;
        --wi-tag-blue-bg: rgba(30, 64, 175, 0.3);
        --wi-tag-blue-color: #60a5fa;
        --wi-tag-slate-bg: #334155;
        --wi-tag-slate-color: #cbd5e1;

        --wi-platform-section-border: rgba(30, 41, 59, 0.5);
        --wi-platform-card-bg: #18181b;
        --wi-platform-card-border: #1e293b;
        --wi-platform-header-color: #ffffff;
        --wi-platform-text-color: #94a3b8;
        --wi-platform-icon-color: #ffffff;
        --wi-platform-icon-color--notion: #e2e8f0;
        --wi-platform-icon-color--square: #ffffff;

        --wi-wp-guide-bg: rgba(15, 23, 42, 0.5);
        --wi-wp-guide-border: #1e293b;
        --wi-wp-badge-a-bg: #334155;
        --wi-wp-badge-b-bg: #312e81;
        --wi-wp-badge-b-color: #a5b4fc;
        --wi-wp-option-title-color: #ffffff;
        --wi-wp-text-color: #94a3b8;
        --wi-wp-step-color: #94a3b8;
        --wi-wp-alert-amber-bg: rgba(120, 53, 15, 0.2);
        --wi-wp-alert-amber-color: #fde68a;
        --wi-wp-alert-amber-border: rgba(146, 64, 14, 0.3);
        --wi-wp-alert-green-bg: rgba(6, 78, 59, 0.2);
        --wi-wp-alert-green-color: #a7f3d0;
        --wi-wp-alert-green-border: rgba(6, 94, 63, 0.3);
        --wi-wp-alert-icon-emerald: #34d399;

        --wi-benefits-bg: rgba(9, 9, 11, 0.5);
        --wi-benefits-border: #1e293b;
        --wi-benefits-title: #ffffff;
        --wi-benefits-desc: #94a3b8;
        --wi-benefits-icon-wrapper: rgba(49, 46, 129, 0.3);
        --wi-benefits-icon-color: #818cf8;
        --wi-benefits-item-title: #ffffff;
        --wi-benefits-item-desc: #94a3b8;
        --wi-benefits-article-title: #ffffff;
        --wi-benefits-platform-card-bg: #18181b;
        --wi-benefits-platform-card-border: #1e293b;
        --wi-benefits-platform-name: #cbd5e1;
    }

    .wi-layout {
        display: flex;
        flex-direction: column;
        min-height: calc(100vh - 60px);
        overflow: hidden;
        position: relative;
        background-color: var(--wi-bg);
    }
    @media (min-width: 1024px) {
        .wi-layout {
            flex-direction: row;
        }
    }

    .wi-main {
        flex-grow: 1;
        display: flex;
        flex-direction: column;
        overflow-y: auto;
        position: relative;
    }

    .wi-content {
        padding: 1rem;
        padding-bottom: 8rem;
    }
    @media (min-width: 1024px) {
        .wi-content {
            padding: 2.5rem;
            padding-bottom: 8rem;
        }
    }

    .wi-content-inner {
        max-width: 64rem;
        margin: 0 auto;
    }
    .wi-content-inner > * + * {
        margin-top: 2rem;
    }
    @media (min-width: 1024px) {
        .wi-content-inner > * + * {
            margin-top: 4rem;
        }
    }

    .wi-section-group {
        margin-top: 5rem;
    }
    .wi-section-group > * + * {
        margin-top: 4rem;
    }

    .wi-canvas--white {
        background-color: #ffffff;
        border-color: #e2e8f0;
    }

    .wi-canvas--black {
        background-color: #000000;
        border-color: #1e293b;
    }

    .wi-canvas--grid {
        background-color: #f8fafc;
        border-color: #e2e8f0;
        background-image: linear-gradient(to right, rgba(128, 128, 128, 0.067) 1px, transparent 1px),
            linear-gradient(to bottom, rgba(128, 128, 128, 0.067) 1px, transparent 1px);
        background-size: 14px 24px;
    }

    .wh-theme-btn--active {
        background-color: var(--wi-theme-btn-active-bg);
        color: var(--wi-theme-btn-active-color);
    }

    .wh-theme-btn--inactive {
        color: var(--wi-theme-btn-inactive-color);
    }
    .wh-theme-btn--inactive:hover {
        background-color: var(--wi-theme-btn-hover);
    }

    .custom-scrollbar::-webkit-scrollbar {
        width: 4px;
    }
    .custom-scrollbar::-webkit-scrollbar-track {
        background: transparent;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: rgba(148, 163, 184, 0.2);
        border-radius: 10px;
    }

    .active-bg {
        border-color: var(--wi-utility-btn-active-border) !important;
        box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.15);
    }

    .active-tool {
        color: var(--wi-utility-btn-active-color) !important;
        border-color: var(--wi-utility-btn-active-border) !important;
        background-color: var(--wi-utility-btn-active-bg) !important;
    }

    @media (min-width: 1024px) {
        .active-tool {
            background-color: var(--wi-utility-btn-active-bg-lg) !important;
            border: none !important;
        }
    }

    .active-color {
        outline: 2px solid var(--wi-utility-btn-active-border);
        outline-offset: 2px;
    }
</style>
